# 阅读排版·字体·手写小注

> 从 `CLAUDE.md` 拆出的功能笔记（2026-08-06 拆）。**动这块代码之前先整本读一遍**——里面每条 ⚠️ 都是踩过的坑。

- **⚠️划线色板里的「金(gold)」已换成「紫(purple)」**（2026-08-10 用户：「可以把那个黄色换成紫色哦，这样最完美了，我应该是不会用那个黄色的」）。起因是夜间主题钉死蓝金、强调色本身就是金黄，划线的黄跟它撞。
  - **⚠️gold 的整套定义一律留着别删**（`--hlc/--hla/--mk-ink`、下划线色、`.rd-gl-gold`、`.rd-sw-gold`、`RD_HL_SW`，以及那两条写死色名的正则里也要继续认它）：书里**已经划过的黄色划线**还得正常显示、还得能被换色正则匹配到。只从 `RD_HL_COLORS` 数组里拿掉就够——色板和随机都只读这个数组。回归 `tests/selbar.test.js` B8/B9 专钉这两件事。
  - 紫的色值：底色 `--hlc: 186,150,250`、记号描边 `--mk-ink: #6b46c1`(夜 `#b9a3f5`)、小注墨色 `#6b46c1`（对比度 6.3，跟其余五支 5~6.6 同一档）、色板圆点 `#a78bfa`。
  - **紫不做任何区别对待**：小注和背景词两边的随机都照常抽得到它（用户 2026-08-10 明确：「我是对黄色区别对待，但是对紫色没有」）。

- **共读正文可调**：正文字号（`--reading-fs`/`reading_font_scale`）、段间距（`--reading-pspace`/`reading_para_space`，2~28px 默认8）。段落带行内 `margin:0.5em 0`，改间距的 CSS 必须 `!important` 才盖得过行内样式。批注框 `blockquote[data-cp]` **只吃自身上边距、贴住它点评的那一段**，不随段间距推开。

- **手写小注（2026-08-04/05 做）**：选中一个字/词 →「✍️ 注」→ AI 回一句 `拼音·意思` → 用中文花体写在段间的缝里，一根手绘引线指回那个词。用户原话「有些个别的字我不会读，或者不知道意思，不需要用一整段话来介绍」。
  - **零新增存储**：一条小注＝一条划线（`reading_highlights`）的 `note` 字段。云同步/换设备/删除/读痕面板全部白拿，颜色也复用划线那 5 个。
  - **一行装不下才折第二行（2026-08-06 做）**：用户看了 neat-annotations 那个库后提的，原话「大部分情况下是一行，极少数的情况下，为了不截断，可以接受两行，同时显示效果也跟之前得是一样的」，选的路线是「先榨干一行，不够再两行」。两步：① 额度——`RD_GL_MEAN` 16→**22**（实测一条意思正好 14 字的典型小注才 239px、**一行的 358px 只用掉 67%** 就开始砍了）、`RD_GL_MAX` 26→**44**（≈一行半；不给到满两行 58 是怕「极少数」变成「动不动」）；② 折行——`rdGlLayout` 里量完天然单行宽，`lw > cr.width` 才就地改 `white-space:normal`。**一行装得下的完全不碰**，宽高与改前分毫不差（96×14 / 239×14 实测两版一致）。回归 `tests/glosswrap.test.js`。
    - **⚠️⚠️必须定死 `width`，绝不能用 `max-width`**（实现时真踩到）：标签是绝对定位的，`max-width` 之下的可用宽 ＝ 容器宽 − `left`，而 `left` 是**量完之后**排位那步才刷上去的 → 量到一行 14px、写完 `left:200px` 当场重折成两行 27px，后面「塞不塞得下 / 压不压到字」全按错的高度算，小注掉出缝外。`tests/gloss.test.js` 的 S4 抓到过（内缩 +7px → −6px）。
    - **⚠️折完要按最长那一行把盒子收窄**（`Range.getClientRects()` 逐行取最大宽）。不收的话盒子恒等于容器宽，排位那步一点横向避让余地都没有。
    - **⚠️`.rd-gl-py` 锁 `white-space:nowrap`**：`míng xiū zhàn dào` 里有空格，允许折行后浏览器会挑空格断开，出来是「míng xiū／zhàn dào·表面…」，一个词被腰斩，比截断还难看。
  - **⚠️「只压隔壁一行」那条退路只对批注/背景块成立，正文段落绝不能用（2026-08-07 修）**：`_insetToward` 第 ③ 档会把标签**整个往上抬一整行**，好让它只压到隔壁块的最后一行。这招对背景块有效（它第一行往往只有「汐：背景：阳翟」十来个字、右边空一大片，抬上去横向一躲就干净），但**正文段落的行是满宽的、无处可躲**，抬上去就成了骑在上一段正文上。用户截图报「那条小注位置咋那么高呀」，追问后原话「可以移到上面，但是上移的位置不应该那么夸张，明明在两段之间就可以」。现在 `_isBodyPara(nbEl)` 为真时改成 `max(RD_GL_INSET_MIN, (缝宽-标签高)/2)`——宁可在窄缝里蹭几个像素，也不许高一整行。回归 `tests/glosshigh.test.js`。
    - **⚠️这个坑只在缝装不下时发作（段间距 < 约 28px）**，段间距 28 走第 ① 档完全看不出来。所以测试必须**逐档段间距**跑，只测 28 会假绿。实测旧代码在 20/14/8 三档都往上顶 32px，修后分别是 7/12/18px（含旋转外框那 5px）。
    - **⚠️别把 28 当成用户的实际值——默认是 8**（`index.html` 里 `localStorage.getItem('reading_para_space') || '8'`；28 是滑块**最大值**）。2026-08-07 她说「我不会改段间距的，一直默认28」，我据此判断「她在 28 档、这个修复对她无效」，**错了**：她在 8 档，正是这个 bug 最严重的一档，修完她当场说「位置恢复正常」。**问用户设置值只能当线索，实际值以代码默认值/实测为准。**
    - **⚠️两行必压隔壁段的第一行字，这是几何死限、不是 bug**：两行高 27px、加上下留白要 41px，而段间距滑块最大只有 28px，差 13px。横向也躲不掉（正文段落第一行通常满宽，不像背景块第一行只有十来个字）。用户 2026-08-06 明确接受了这个代价。
    - **⚠️副作用（明知故犯，已告知用户）**：`RD_GL_MAX` 同时是「用户手写批注写成小注 vs 收进 ✎ 小点」的门槛，分流处拿不到「这条是 AI 注还是自己写的」（同一个 `note` 字段），所以门槛一提，27~44 当量的手写批注也从 ✎ 变成两行小注。
  - **⚠️⚠️小注和引线必须挂在**整个正文容器上单独一层（`.rd-gl-layer`）、**绝不能进 `<p>` 里**。点击翻页 `readingPageDown` 靠 `range.selectNodeContents(p).getClientRects()` 逐行量位置，塞进段落的绝对定位元素会被当成「一行」、翻页翻歪——那是读小说时每分钟都在用的手势。
  - **⚠️锚点按「行片段」算，`getBoundingClientRect()` 在这里是错的**：① 选中的字跨过人名高亮会被拆成好几个 mark；② 词本身跨行折断（「诸侯皆缟／素」）时那个大框的中心落在两行之间的空白里，箭头直接指飞。收齐所有 `getClientRects()` 片段 → 按行分组 → 往上锚第一行、往下锚最后一行。
  - **⚠️永远不许把段间的缝撑高**（用户明确：「不要把那个缝撑高好吗，我担心会影响到行号什么的」）。放不下就换另一条缝、或者往左让位，**不改版式**。上下有多少空当**必须实测兄弟节点**、不能假设就是段间距——紧跟段落的批注框 `blockquote[data-cp]` 不吃段间距，那道缝只有 ≈5.6px。
  - **⚠️「这一行满了」＝已经摆好的那条挡住了，不是「够不到右边界」**（2026-08-05 修）。`_gl_try` 原本把「想放的位置伸出段落右边」也算满 → 另起一行 → 整条抬高约 28px 压进上一段正文。用户报的那条只超出 **3px**，而那条缝里就它一条。「离目标字 22px」只是手感偏好、本来就该让位；先贴右边界再试，撞了才换行。**越宽的小注越容易踩**，所以症状像随机发作。回归 `tests/gloss.test.js` S 组。
  - **⚠️拼音和中文是两种花体，按「·」切开**：拼音锁死用「溪涧山雪璞玉浑金」（唯一声调齐全的），中文用她偏爱的「青春例外你是偏爱」（缺字回退溪涧）。**别写成「青春优先、逐字回退」**——青春是有 a~z 的、只缺 17 个带调元音，那样 `shēng` 会变成 sh(青春)+ē(溪涧)+ng(青春)，一个词三种笔迹。
  - **⚠️墨色和划线底色是两套颜色、互不影响**（2026-08-05 修）：底色是 `mark.rd-hl.rd-hl-*` 的 rgba 背景（用户说日间的底色**刚好、别动**），`.rd-gl-*` 只管笔迹。日间原本借用「下划线样式」那 5 个色，但那套是给一条线调的，当花体字（笔画细 + opacity）在浅背景上太淡——金色对比度只有 **2.0**。现日间单独压深到 5~6:1 且不打透明度折扣，**夜间维持原样**（用户满意）。
  - **⚠️⚠️拆 DOM 壳子一律「把孩子搬出来」，别用 `textContent` 压平（2026-08-05 修）**：用户报「先写的小注，标背景之后那个被解释的词的小注就不见了」。根因在 `readingRehighlightNamesInPlace` 拆旧人名 span 那句 `replaceChild(createTextNode(s.textContent), s)`——**划线 `<mark>` 正套在 `.reading-name` 里面**（标背景收尾会把讲过的词并进名单高亮，而她写小注的正是同一个词，两件事撞在同一个词上），一压就把 mark 连同 `data-gl` 一起抹掉，**划线和小注一起消失**。**⚠️数据一直是好的、只是 DOM 被抹**，重开一章又回来，所以看着像随机发作、也容易误判成「小注排版丢条」。**排查记录（省得再走一遍）：先后否掉了「词进名单挤掉小注」「背景块占了段间那道缝导致丢条」「被背景块盖住」三个猜测，都是实测否的**——真正定位靠**跑真的 `readerMarkBg` 循环**（假请求）而不是手工插块，修前「划线 mark 数 1→0」当场现形。回归 `tests/gloss.test.js` U 组（4 条；A~T 已被占满，新组从 U 起）。
  - **⚠️引线交叉的第二种成因：排序键里混进了标签宽度（2026-08-05 修）**。第一种是「舍近求远」（跨行的词只算一份 want，已修）。这次是用户截图《史记的读法》「…**撮**名法之要…」：`撮`(小注宽 89) 和 `名法`(小注宽 156) 两个字才差 21px。摆位是「按排序从左往右占位」，而老排序键写的是 `字x + 方向偏移 − 标签宽/2`（＝标签想放的位置）——**宽的减得多**，于是宽的那条被排到前面、先占左边，字是左→右标签却右→左，必然交叉。**排序键只能用目标字的位置；标签宽度决定「摆在哪」，不决定「谁先摆」。** 修完那两条会自动分到两条缝（放不下就换缝是既有退路，比挤在一条缝里交叉好）。回归 `tests/gloss.test.js` V 组。
  - **⚠️⚠️选中文字那条小条上「没有色板」，颜色是随机的（2026-08-10 大改）**。用户原话：「刚开始选中的时候，没有那些颜色条，它可能是随机的小注颜色，但是选好之后再点，就会出现可选颜色，这个时候我可以按我自己的想法去选颜色，这样就即兼顾了随机又兼顾了自定义」。
    - 选中小条现在只有三颗键：**🖍划线 → ✍️注 → ✏️改**。想指定颜色 → 划完**再点那条划线**，色板在 `rdHlShowEditBar` 那条上（那条一个零件没动）。
    - 随机取色 `rdHlRandomColor`：⚠️**避开 ink**（中性灰/白，当「随机惊喜」太素，它是留给手动挑的）；⚠️**躲开上一次那支**（人对连号特别敏感，连着同色就不像随机了）。「✍️ 注」也改成随机色——它原来跟着 `reading_hl_last_color` 走，那是「选中就有色板」时代的遗留，现在没色板可点，跟着上一次等于永远一个色。
      - **⚠️两条创建路径都必须把颜色写回 `reading_hl_last_color`**（`rdHlCreate` 和 `rdGlMake`）：「躲开上一次那支」全靠这个键。小注那条改成随机取色时**漏了写回**，于是每次都拿一个陈旧的值去比，用户当场发现「感觉有点连着，就是同一种颜色可能出现两次」（2026-08-10）。⚠️测这件事**不许手动 setItem 去喂**——那只能证明函数本身没问题，必须走真实创建流程让代码自己写回，见 `tests/selbar.test.js` D 组。
    - **⚠️连带撤掉了「划线样式(底色↔下划线)」那颗键**：它显示的字正好也是「划线」，跟新的真·划线键撞脸——那颗键历史上就害用户以为「划线功能坏了」（见 `rdHlStyleBtnHtml` 上那段⚠️⚠️）。它在**已有划线**那条小条上还在，功能不丢。
    - 于是 2026-08-04 定的「色板 → ✍️注 → 划线样式 → ✏️改」那个顺序**作废**，`tests/gloss.test.js` H3e/H3f 已改钉新顺序。回归 `tests/selbar.test.js`（15 条）。
  - **浮动小条上没有「问」**（用户几乎不用，两个条上都撤了）；「✍️ 注」放在**色板后、划线前**（高频功能，位置是她定的）。常见字可以不写拼音。

- **共读点击翻页按行对齐**（`readingPageDown`）：用 `Range.getClientRects()` 找「本页最后一行完整露出的行」滚到容器顶部——新页顶行 = 上页读到的最后一整行（保留一行接头），永远不切半。找不到行或目标太近(<60px)退回整屏滚。极小瑕疵：开「画框」时顶边可能露上一行画框底描边 2~4px，文字不切。

- **精句样式（6 个开关，`reading_ks_*` → body 上 `reading-ks-*`）**：画框/波浪/柔光 铺句底三选一；划线与波浪/画框互斥；着色、加粗可与任意叠加。入口 `readingToggleKeySentStyle` / `readingApplyKeySentStyles`。
  - **⚠️只有「着色」按日夜各记一份**（2026-08-07，用户原话「夜间模式的时候精句默认着色，日间模式的时候精句取消着色」）：键是 `reading_ks_hl_night` / `reading_ks_hl_day`，默认夜开日关，老的共用键 `reading_ks_hl` 不再读。**其余五项仍共用一份，别顺手拆**——她只提了着色。
  - **⚠️她选的是「按日夜各记一份」而不是「模式说了算」**：手动改只影响当前模式、且切走再切回还记着。别图省事改成切模式无脑覆盖，`tests/kshlmode.test.js` 第 ④ 条专钉这个分水岭。
  - **⚠️判日夜用 body 的 `dark` 类，不用 `nightMode` 变量**：有的皮肤本身就是深色（THEMES 里 `cls` 直接带 dark），那时 `nightMode` 是 false 但用户眼里就是夜间。
  - **⚠️挂钩在 `applyTheme` 末尾**（切主题、切夜间都走它），且必须放在那批 body class 增删**之后**——放前面会读到上一个模式的值，表现是「切过去要再切一次才对」。
- **字体槽位（7 个）**：`READING_FONT_SLOTS` = 正文/引号/短句/标题/人名/精句 + **缺字**。前六个各写一个 CSS 变量；「缺字」（`isFallback:true`）不写变量，而是接在**装饰槽位**字体链尾巴上（`"主字体", "缺字字体", serif`）——**正文那条链不接**，用户明确要求正文一个字不动。花体做子集常漏生僻字（「苼」在 GBK 不在 GB2312，实测国产花体基本都不收），浏览器逐字回退，主字体有的字轮不到兜底。槽位选「默认」时**照旧不写变量**：靠 CSS `inherit` 继承父级算好的整条链，兜底会跟着继承过来，在这里补反而会切断继承。
  - ⚠️ **加新槽位必须同时改两处**：`READING_FONT_SLOTS` 和 `cfBuildManifest()` 里那个**手写死的** `fontSlots` 对象（不是从槽位表推的）。只加前者的话，`readingApplyFontSync` 会在云端较新时把新槽位 `removeItem` 掉，表现是「当场好好的、过几小时自己消失」，肉眼根本查不出来。测试 `tests/font.test.js` A 组钉住。
  - **⚠️「标题字体」只作用在 `.reading-merged .reading-chap-title` 上，而这个 class 原本只靠 `READING_TITLE_RE` 猜（2026-07-30 修）**：那条正则要求同时满足「`第X章/节/回/篇/卷/话` 开头 + 后面 ≤20 字 + 不含逗号句号问号顿号」，所以**凡是不长成传统章回体的章名一律拿不到标题字体**——用户导进来的知乎存档三本，章名是「【商业经济】教育家 - 为什么，这几年…？」，连改写成「第一章 为什么，…？」也照样不认（有逗号问号）。**数据侧无解**：要过正则就得把章名砍到 20 字内且不带标点，那既看不出讲什么、也搜不到了。修法是**阅读模式不用猜**——`readerChapterPair` 本来就把章节标题拼在正文最前面（「标题占掉 P1」），所以 `chatReaderMode && p === 0` 直接认，和原正则**并联**（共读模式一字不动：那边正文是发进来的，P1 不保证是标题）。⚠️另加 `length <= 120` 兜底，万一某本书 P1 不是章名而是正文首段，别把一整段套上花体糊成一团。⚠️**`_readingMergeCache` 的签名必须带 `rdMode`**：同一段文字在两种模式下该不该包这个 class 不一样，不带会串出**隐形的错样式**。测试 `tests/chaptitle.test.js`（7 条）。

- **⚠️「小注怎么跑进背景块里去了」不是 bug，别去修（2026-08-10 用户截图问过一轮，结论是维持原样）**：她的原话「下面的两个小注好像偏要在背景块的区域，为啥呢，因为我看留出挺多空隙来着，是不是担心压到正文呀？」。三层原因，下次照抄就行：
  - **下缝物理上根本装不下**：段落 `margin-bottom: 0`，背景块只吃自身 `margin: 0.4em`（≈5~6px），而一条小注连斜着的外框要 **21px**。她眼里那片「空隙」是**背景块自己的 `padding: 0.5em`**（块的身体），不是缝。
  - **上缝天生比下缝宽**：块的下边距 `0.6em` 还要跟段间距取大者，所以「块→段落」永远比「段落→块」宽。截图里"正常"的那两条只是正好走了宽的那侧；同一张图上面那个块里其实也躺着一条，是同一回事。
  - **落进块里是「挑」出来的、不是掉进去的**：目标字在段落后半截，往下近得多，而 `_stuck` 实测出「背景块第一行只有十来个字、右边一大片空地，横向一躲一个字都不碰」→ 按「不遮字优先、其次就近」留在了下面。硬往上挪反而要跨半段正文去指（＝她 2026-08-07 报的「位置咋那么高呀」）。
  - **换设备会自动让开**：这些量都是每次排版现场测的。窄屏/大字号下背景块首行被撑到满宽、右边没空地了，`_stuck` 就判成真压字，这条会自己翻到上面的缝去。她当场自己想到了这一点（「换个设备没有这个空隙，就很容易压到字了」）——答案是不用担心。
  - **唯一的改法**是把块的 `margin-top` 从 `0.4em` 提到约 `1.5em` 给小注腾缝，代价是**所有**背景块都跟正文拉开一道明显空档、整篇变松。**已问过、她选了不改**（"这样挺好…这个设置本身应该是ok的"）。

- **引线改虚线（2026-08-10 用户要的，原话「你有办法把线弄成虚线吗，我感觉那样效果会更好一些」）**：`.rd-gl-svg path` 上一条 `stroke-dasharray: 3 3.6`。
  - **⚠️⚠️箭头必须是**独立的第二条 path**，绝不能合回曲线里**：原本 `rdGlPath` 返回一整条 d（`M…Q…` 接着 `M…L…L…`），箭头那两笔总共才 `HL=6.5px`，跟着虚线一切就碎成几个孤零零的小点、**箭头当场消失**。现在 `rdGlPath` 返回 `{d: 曲线, a: 箭头}`，画成两个 path，箭头那条挂 `.rd-gl-arrow` 并用 CSS `stroke-dasharray: none` 关掉虚线。**两条都要刷 `style.stroke`**，否则线是彩的、箭头是黑的。
  - **⚠️曲线必须先 `appendChild`**：`tests/gloss.test.js` 和别处多处按 `.rd-gl-svg path` 取「第一条」再用 `^M… Q…` 解析 d，顺序反了会取到箭头、正则返回 null 当场抛（O 组就是这么红的）。凡是**遍历/计数** path 的地方都要写成 `path:not(.rd-gl-arrow)`（已改：O 组、M3）。
  - **⚠️间隔要比线段长**：`stroke-linecap: round` 让每段两头各鼓出半个线宽（0.7px），写等长的 `3 3` 实际是「段 4.4 / 隙 1.6」，看着几乎还是实线。别为了省这点把 linecap 改成 butt——圆头正是它像手写笔触的原因。
  - 回归 `tests/gloss.test.js` **X 组**（6 条，钉住拆分/顺序/曲线虚/箭头不虚/同色）。⚠️A~W 都已占满，**下一个新组从 Y 起**。

- **小注那个词的「手绘记号」（2026-08-10 做）**：写过小注的词不再显示划线底色，改画一个手绘记号——**u 下划线 / b 方框 / c 圈 / h 荧光**。用户看了 [rough-notation](https://roughnotation.com/) 之后要的，原话「下划线，box，circle，highlight我都喜欢哎」，并自己划定了适用面：「适合在长度比较短的单字或者词语上面」。入口 `rdMkImg`（生成抖动 SVG）/ `rdMkPick`（挑哪种）/ `rdMkApply`（打到 mark 上）/ `rdMkClear`；开关 `rdMkToggle`（设置面板「小注 · 手绘」，键 `reading_gl_mark`，**默认开**）。回归 `tests/glossmark.test.js`（34 条）。
  - **哪个词用哪种＝字数定范围 + 范围内钉死的随机**（她的原话「我想着随机一点呢，要不按字数来定」，最后定的是两者分工：字数管能不能，随机管是哪个）。1~4 字四种都行；≥5 字只在下划线/荧光里挑（长词画圈＝一个大扁椭圆）；**跨行的词一律降级成下划线**（圈/框是绝对定位伪元素，只认第一段行片段，跨行会裂成两个半圆）。
    - **⚠️随机必须钉死**：种子取那条划线自己的 `hlid`，同一条小注刷新/换设备/重开一章永远同一个记号。每次打开都换＝页面在抽风。
  - **⚠️⚠️四种记号一律零 padding，左右上下都不许加。** 左右的理由是老的（mark 是行内元素，横向 padding 会推开整段正文、还带偏小注引线锚点，见 `mark.rd-hl` 那条）；**上下是这次实测新加的**：行高确实没变，但 `Range.getClientRects()` 量到的行矩形跟着涨 1px，而点击翻页 `readingPageDown` 正是靠它找「本页最后一整行」——一条小注就能让翻页的接头偏掉。`tests/glossmark.test.js` E 组当场逮到，并**逐种**验（四种画法不同，只测随机抽中的那一种会假绿）。换成 Rough.js 之后更彻底：**形状全部画在 SVG 层上，mark 上那个 `rd-mk` class 只干一件事——撤掉底色**，一个 padding 都没有。
  - **⚠️⚠️形状是**真的 Rough.js** 画的（4.6.6，MIT，内联在 index.html 顶上那个独立 `<script>` 里，约 28KB）。第一版是我自己拿 `feTurbulence` 滤镜揉的抖动图，用户看了对比图当场否掉：「你这完全没按rough notation来呀，纯是你自己弄的吧」——**她是对的**：Rough 的手绘感来自「同一条线来回描两遍、每遍都歪一点」，滤镜只是把一条规整的线整体扭一下，远看都叫手绘，近看一个是素描、一个是水波纹。**别再改回滤镜版。**
    - **⚠️为什么不用 `rough-notation` 那个上层库**：它把画好的 SVG 挂在页面最外层、按窗口坐标定位，而阅读器的正文一直在滚——滚一下记号就跟字分家（做对比图时实拍到下划线跑到下一行）。我们只用底下的 Rough.js 引擎，**画到自己那层 `.rd-mk-svg` 上**，坐标相对正文容器，跟着重排/换主题走。
    - **⚠️记号必须单独一层 SVG，不能跟引线共用**：引线那层有 `.rd-gl-svg > path` 的 CSS（虚线/1.4 宽/圆头），记号的线落进去会被一起套成虚线。分两层还顺带定了叠放次序：记号在下、引线在上，粗荧光不会压掉箭头尖。回归 G6~G8 钉住。
    - **⚠️`seed` 必须由 hlid 算好传进去**：Rough 每次画都重新随机，不给种子的话每次重排（换字号/切主题/翻页重渲）同一个圈的歪法都不一样，看着像页面在抖。回归 I 组。
    - **⚠️多遍描边合并在同一条 `d` 里**（好几段 M 开头的子路径），`<path>` 元素通常只有一个——想验「描了两遍」要数 d 里的 M，别去数 path 个数（写测试时踩过）。
  - **颜色**：描边取 `--mk-ink`、荧光取 `--hlc`/`--hla`，**三个变量都定义在 CSS 的划线色板那几行里**（日间深色、夜间翻成浅亮色），JS 用 `getComputedStyle` 现取。**加/删划线颜色时只改那几行就够，别再去 JS 里同步色表。**
  - **荧光＝原色带的手绘版，浓度一点没动**（她定的「直接替掉」）——只把两端改成不齐、边缘微抖。她说过日间底色浓度刚好、别动，这条仍然成立。
  - **⚠️底下已经有别的下划线时，不许再画 rough 的下划线**（2026-08-10 用户报：「有下划线（来自断句精句这类样式）的情况下，最好不要再用rough的下划线，不然有点容易重叠」）。三个来源，都要**开关＋位置**同时成立才算：精句划线(`reading-ks-ul` + `.reading-keysent`)、引号划线(`reading-uline-on` + `.reading-quote`)、名词划线(`rn-dash` + `.reading-name`)，见 `rdMkHasUnderline`。长词把 u 去掉后只剩荧光，这是对的（框和圈本来就不给长词）。回归 J 组。
  - **⚠️⚠️荧光必须用 `mix-blend-mode: multiply`（夜间 `screen`）**：记号这一层画在正文**上面**，半透明色直接盖上去会把黑字调浅——用户报「荧光笔似乎是盖在字体上的，像一层纱使它显得有点糊，我觉得你可以参考一下之前的那个色带，因为色带盖在上面没有影响字的本色」。她说得对，原色带是 `background`、长在字后面。正片叠底能拿到同样的效果而**不必把这层塞到文字下面**（塞下去要动 z-index，会被祖先的气泡背景盖掉）：彩色×黑字＝还是黑字，彩色×白纸＝那片彩色。夜间底是暗的，正片叠底会把荧光压成黑的，所以翻成滤色。回归 K 组。
  - **⚠️荧光(h)已被用户去掉**（2026-08-10「去掉荧光笔效果」），池子只剩 u/b/c 三种。画法留在 `rdMkDraw` 里没删（它是唯一「不遮字又能铺一片」的画法），**但别自作主张加回池子**。副作用：长词（≥5 字）现在只剩下划线，若同时「底下已有下划线」，`rdMkPick` 返回 `null` ＝ 不画记号、保留原来的划线底色。
  - **普通划线**（不带小注的）当时明确说先不做，别顺手铺开。

- **「背景词」的手绘记号（2026-08-10 做）**：标背景讲解过的那个词，在正文里画一个手绘记号。入口 `bgMkLayout` / `bgMkLayoutAll` / `bgMkTermOf` / `bgMkRectsOf`，开关 `bgMkToggle`（设置面板「背景词 · 手绘」，键 `reading_bg_mark`，**默认开**）。回归 `tests/bgmark.test.js`（19 条）。
  - **⚠️⚠️先做成了「满篇人名」，用户当场否掉**，原话：「我想我其实并不是要让每个人名都要被渲染成那样，而是在有背景词解释的那个段落，把那个词儿给他渲染出来就行，不然确实会很花」。实拍确实花——**一行三个圈几乎连成一片**。**别再扩回所有人名。**
  - 词从哪来：背景块是 `blockquote[data-bg="1"]`，第一行 `<div class="reading-q">汐：背景：孙武</div>`，块上的 `data-cp` 指着它讲的那一段。抠出词 → 回到那段找它**第一次**出现的位置 → 画。⚠️只认第一行（正文里也常出现「背景：」字样）；⚠️「汐」是可改的昵称，按最后一个「背景：」切，别写死；⚠️词长 >8 字不画（AI 偶尔把整句当题目，圈一整句既难看、段落里也多半找不到原样文字）；⚠️段落里找不到那个词就**安静跳过**。
  - **颜色「一词一支笔」**（用户原话「人名上的颜色是随机变的」）：六色板定义在 CSS 的 `--bg-ink-1..6`（日间深、夜间浅），按词哈希取。同一个词全书同色同款。
    - **⚠️⚠️取变量必须用 `getComputedStyle(document.body)`，不能用 `document.documentElement`**（2026-08-10 实测逮到）：夜间那套定义在 `body.dark` 上，而 CSS 变量只往**下**继承——从 `<html>` 上读永远只拿到 `:root` 那套日间色，**切夜间等于没换色**。光看代码「两套都写了」根本看不出来，是用户问「你有没有对颜色做日间模式和夜间模式的相适应」才查出来的。回归 `tests/bgmark.test.js` G 组钉死。
    - **⚠️那支黄现在不参与随机**（`BG_MK_INKS = [2,3,4,5,6]`，2026-08-10 用户：「背景词的随机颜色中，也把那个黄色出现的可能去掉吧，因为夜间我主色调是黄色，容易重复」）——夜间主题钉死是蓝金，强调色本身就是金黄。**CSS 变量和它的加粗设定都留着**，想加回来只要把 `1` 放回 `BG_MK_INKS`。⚠️这两个常量必须放在 `bgMkLayout` **外面**：第一版写在函数里、还写在用它那行的后面，`var` 声明提升赋值不提升 → `undefined.length` 当场抛。
    - **⚠️那支黄的来历（留着备用，别删）：荧光黄，日间 `#eec400` / 夜间 `#ffe500`，且单独加粗到 2.1**：我第一次理解成「要更深」，压成了 `#b07d00`，被用户当场纠正「颜色要改成荧光黄色那种」。它配纸白对比度只有 1.15、按无障碍标准不合格，但要的就是荧光笔划过纸的观感，是她明确的审美选择。为了不发飘，**这支笔单独加粗**（`inkWidths[0] = 2.1`，其余 1.5）——荧光笔本来就比钢笔粗，观感反而更对。
  - **⚠️同一段里出现几次就画几次**（2026-08-10 用户报：「一段里面居然有多个背景词，相同的背景词哈，居然只划里面的一个」——第一版只 `indexOf` 一次就收工）。`bgMkAllRectsOf` 循环找完所有出现；⚠️每一处给**不同的 Rough 种子**（`seed + gi*7919`），否则同一个词的几处歪法一模一样，像复制粘贴。同一个词有**两个背景块**时仍只处理一次（`seen[term]`），别把同几处各画两遍。
  - **记号只有框/圈/下划线，且下划线有「三个字」的门槛**（`BG_MK_U_MIN`）。用户两次定的：先说「对背景词这个来说，下划线可以去掉，还是画圈画框比较好」，随后补「下划线还是可以保留，但是得是三个字及以上」——短词底下一条短线看着像笔误。⚠️跨行的词只能用下划线（每行一条、天然不裂）或方框；圈跨行会裂成两个半圆。⚠️底下已有别的下划线（精句/引号/名词的「划线」）时避开 u，同小注那条规则。
  - **⚠️形状和颜色必须用两个不同的哈希**：形状是 `seed%3`、颜色是 `seed%6`，而 6 是 3 的倍数——共用一个 seed 等于把两者绑死，六支笔只剩六种固定搭配（实拍：两个不同的词一模一样都是紫圈）。颜色用 `rdMkSeed(词 + '·色')`。
  - **⚠️`rdMkSeed` 末尾那三步「雪崩混合」不许删**：光是 `h*31+c` 对短字符串的低位分布很差，而我们全靠 `%3`/`%6` 取值——实测 12 个常见词里 6 个形状撞在一起、「孙武」和「豫章」连颜色都一样。回归 `glossmark` A6 钉住。
  - **性能**：Rough 画 600 个记号实测 23ms（这台机器），手机上大概 100ms 出头、一次性。所以这条路能用 Rough；但**别把这套挪去做「所有正文字」那种量级**。
  - **⚠️⚠️背景词那一层必须有自己的 class 和定位样式，绝不能蹭 `.rd-gl-layer`**（2026-08-10 用户报：「当一个段落里面有画框时，我这个时候进行小注，原本因为背景词而被选出来框起来的那一些渲染会消失，我必须退出重进才会重新渲染」）。根因：小注重排第一步 `rdGlClear` 删的是**所有** `.rd-gl-layer`，当时为省几行 CSS 让背景词层也挂了这个 class → 写一条小注就把背景词记号一起抹了，而 `rdGlLayout` 只会重建小注层。现在 `.bg-mk-layer` 自带定位样式，`rdGlClear` 上也留了路标。回归 `tests/bgmark.test.js` H 组。
  - 重排挂钩搭 `rdGlLayoutAll` 的车（字号/段间距/字体/主题/夜间/转屏都接在那儿），另外 `readingApplyNames` 末尾也挂了一次——名词开关一动字重就变，整行的字会位移。
