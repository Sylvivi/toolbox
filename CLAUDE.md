# toolbox 项目记忆

> 本文件是"改这个前端项目"用的备忘，随每次对话加载，请保持精简。
> 服务器/运维（订阅桥、书库服务、Caddy、cron 等）的敏感拓扑**不在这里**——见服务器本地私有文件 `SERVER-OPS.md`（已 gitignore、不入公开仓）。

## 项目概况
- **单文件 HTML PWA**：核心就是 `index.html`（约 2.4 万行 / 约 1.4MB，JS/CSS 全部内联）。**不要拆成多文件**，用户要求保持单文件。
- **⚠️ `#chatMessages` 容器必须保持空**（源码里应是 `<div class="chat-messages" id="chatMessages"></div>`）。它是运行时消息/阅读内容的渲染区，app 启动会清空重渲染——**绝不能让渲染后的正文/点评被冻进源码**。曾有一整段《傲慢与偏见》阅读快照误存进去，白白撑大文件 1.2MB（2026-07-07 已清）。多半是**从运行中的网页整页保存/导出**导致的，改动后留意别把 DOM 快照写回源码；发现文件异常变大先 grep 书中人名/`class="chat-msg"` 排查这里。
- **⚠️同一个坑还会冻进密钥——仓库是 public（2026-07-27 发现并清理）**：`chatKeySelect`/`chatKeySelect2` 里躺过**两把明文 API 密钥**，`chatProviderSelect(2)` 躺过 16 个中转站的名字和 id，`compressModelSelect`/`imageModelSelect` 躺过收藏模型列表——**GitHub Pages 直接对外，查看源代码就能读到**，且在公开 git 历史里躺了两个多月才被发现（排查别的 bug 时顺带看见的）。这些 `<select>` 的内容**全部由 JS 在运行时重建**（`populateChatProviders` / `onChatProviderChange` / `_chatRenderModelSelect` 都是 `innerHTML` 整个重写），源码里**只该留占位项**。旁证：出事的 select 连 `style` 都被序列化成 `flex: 1 1 0%` 这类计算值。**每次改完 index.html 建议扫一眼**：`grep -oE 'sk-[A-Za-z0-9_-]{16,}' index.html`（应为空）+ 检查上述 select 是否只剩占位 option。**密钥一旦进过公开历史就必须去中转站作废重发，删文件没用。**
- **部署**：GitHub Pages，仓库 `Sylvivi/toolbox`、分支 `main`。线上 https://sylvivi.github.io/toolbox/ ，**自定义域**（免梯子）`https://tool.masterofmydomain.top`（Cloudflare CNAME → sylvivi.github.io）。
- **更新机制**：network-first 的 service worker，推送后过一两分钟刷新页面即生效。
- **主要用途**：用户主要用「共读模式」读长篇小说（如天龙八部），其次翻译模式、普通对话模式。
- **PWA 安装注意**：装 PWA 到桌面要**挂梯子**（WebAPK 需 Google 服务器签发、国内被墙）；不挂梯子会退化成「带 Google 标识的快捷方式」，状态栏颜色被冻死、跟不上主题。装完之后日常用不需要梯子。遇到「状态栏不变色 / 图标变样」先往这个方向查。

## 部署排障
- 根目录有 **`.nojekyll`**，让 Pages 跳过 legacy Jekyll 构建、原样发布（曾因反复 Jekyll 构建失败导致线上半天不更新，加它后修好，别删）。
- `sylvivi.github.io/toolbox` 会 **301 重定向到自定义域**，curl 验证线上要么加 `-L`、要么直接打 `tool.masterofmydomain.top`。
- Pages 的「部署」步骤偶尔 GitHub 侧抽风（build 成功、deploy 失败/卡 queued）。**别干等**：立刻推个空提交触发全新运行（常几十秒就过）；服务器上有 `/home/ubuntu/deploy_autoheal.sh` 可后台自动盯着重触发。查真实状态用 Actions API，`/pages/builds` 那个老接口会滞后，别只信它。

## 用户偏好（重要）
- 用户**非技术背景**，请用**中文**回复，方案要**傻瓜式**、解释通俗，少术语。
- **提交习惯：改完代码、验证通过后，直接 `git commit` + `git push`，不用再问「要推吗」。** 用户明确表示不想每次被问。
- 提交信息用中文，遵循仓库已有风格（`fix:` / `feat:` 前缀 + 简述「为什么」）。
- 遇到可能长时间的等待（部署/构建），别让用户守着——用后台脚本自愈、失败就立刻重试，先给结论和处置。
- **注释写厚一点，用户 2026-07-28 明确认可（「继续保持」）。** 目前约 2000 行注释 / 7%，86 处 ⚠️。理由不是风格偏好，是这个项目特有的：① 用户不看代码，反馈只到「第 34 章插到第 33 章了」这种现象层，结论不就地写下来，下次就得从 2.6 万行重新推；② **每条坑都是用户在手机上花额度换来的**（`max_tokens: 4000` 旁边那条警告就是），不写清「为什么不能改回去」，下一个人（很可能是我自己）会觉得浪费又改回去，让她再烧一次；③ 单文件 + 上下文会断，断掉后能读回来的只有注释和本文件。**代价**：注释会过时——改代码时必须顺手校一眼旁边的注释还成不成立（近义去重那次就得回去改「故意不做包含关系」那条）。

## 开发工作流
1. 编辑 `index.html`（仓库在服务器上 `/home/ubuntu/Toolbox`；这台机器同时就是线上宿主机）。
2. 验证：服务器已装 node + playwright(chromium)。**判断标准是「不真去操作一下能不能知道好没好」，不是「改动大不大」**（用户 2026-07-29 定的尺度，她原话「感觉从你说要弄测试什么的之后，每一轮花的时间都变长了很多」）：
   - **不写测试、改完直接推**：改颜色/渐变/字体粗细、换图标、改按钮上的字、菜单项挪位置、改提示文案。读代码确认没有别处依赖即可。
   - **写 headless 脚本实测**（跑 `file:///home/ubuntu/Toolbox/index.html`）：新手势、新按钮的行为、同步/存储/花钱的逻辑、会影响好几处的公用代码。
   - **⚠️测试脚本自己超过 2 次没跑通就停手**——那说明是在跟工具较劲、不是在验代码（真踩过：找不到 playwright 装哪、被浮层挡住点不到、删了 DOM 反而弄出报错、期望值写错两次，每次都要重来一轮，而改动本身十秒就完了）。当场退回读代码确认，别拿修脚本的时间当验证。
   - **⚠️全套回归 `bash tests/run.sh`（约 3-4 分钟）默认不跑，别拿它挡着推送**（用户 2026-08-02 定：「感觉没有必要跑全量回归，你推吧，有问题我跟你说，现在跑全量回归花的时间太长了」）。改动本身的**针对性测试**跑绿了就直接 commit + push，回归留给「她报了个 bug、怀疑是以前哪条护栏被碰掉了」的时候补跑。**别一边推一边后台跑全量**——跑完再回来收拾比一次推干净还麻烦。真跑时只跑一遍，输出直接完整落盘到文件，别 `| tail -30` 再回头补跑。
3. 验证通过 → 直接 `git -C /home/ubuntu/Toolbox commit` + `push`。
4. 凭据存 `~/.git-credentials`（HTTPS + PAT），推送无需交互。

## 关键功能笔记
- **⚠️「汐」在代码里是两种身份，别顺手统一（2026-08-02 加昵称功能时理清）**：用户的朋友也开始用这个工具了，所以昵称做成可改（设置面板「昵称」栏 → `chatUserName()`，存 `toolbox_user_name`、走 CF 同步）。**但只有「给人看的名字」跟着昵称走**（AI 称呼、问答气泡、读痕面板、导航面板，7 处）；**`QA_TAG='汐：'` 是存储格式标记，一个字都不能动**——正文问答以 `[P3] 汐：问题` 存进 `msg.content`/`reader_qa`，二十来处 `/^汐：/` 靠它认出「这块是问答不是点评」。改掉它的后果是**静默的**：正文照常显示，只是双击不再走追问、摘要开始把闲聊当剧情压进去、读痕面板捞不到问答，几千条历史问答集体退化，要等用到那个功能才发现。所以改昵称是**纯显示层**的事，不迁移任何数据、改回去也一样。发给 AI 的副本用 `chatQaTagToName()` 把标记换成昵称（否则系统提示说用户叫小明、上下文里一堆「汐：」，AI 以为是第三个人）——⚠️**它的正则必须连 `[Pn] ` 段号前缀一起吃**，第一版只认行首、一条都没替换到，测试逮到才发现（真机上看不出来，只表现为 AI 偶尔管你叫汐）。云同步判在不在用 `!== undefined`，**空串是合法值**（＝用回默认），真值判断会让「改回默认」永远传不出去。测试 `tests/username.test.js`（23 条）。**顺带记下**：`READING_MODE_DEFAULT_PERSONA` 里还有 228 字男生子题材声明 + 186 字家族树画法，那是用户自己的口味，别人用要自己在系统提示词框里填内容覆盖掉（框里非空就不走兜底人设）。
- **⚠️「第十回排在第一回前面」＝空壳章被正文里那行回目改了名（2026-08-02 修）**：用户报《笑傲江湖（新修版）全4册》目录乱序。**服务器那份数据是好的**，坏在导入管线：`rbDeriveTitleAndBody` 会翻每章正文**前 8 个非空行**，看到像回目的行就拿来当章节名（这是治「epub 把每章 `<title>` 都设成书名」用的，本身有用）。而金庸这套 epub 每卷开头有个**卷首目录页空壳**——`<h1>` 是本卷**第一回**的回目、肚子里只有一行本卷**最后一回**的回目（`第一回 灭门` / 正文只有 `第十回 传剑` 6 个字）。于是那个壳被改名成「第十回 传剑」、正文同时清空，目录里凭空多出个 0 字假章节卡在真正的第一回前面。用户那份还叠了一次「重新拆分」，把 0 字空壳和后面的图辑/新序并成一章，显示成「第十回 传剑 **6,087 字**」。
  - **⚠️这个 bug 在目录上完全看不出来，只有字数暴露它**（假的 6,087 vs 真的 24,084）。写这类断言别只比标题，要比「有没有正文空了却还叫着回目名的章」。
  - **修法**：揪完正文空了 ＝ 那行是这一章**仅有的内容**、不是它的标题，保留原标题（`headIdx >= 0 && !newBody.trim() && t`）。只兜「从正文里揪的」这一路，不动 `_rbIsBareChapterNum` 那条正常路，**章数一个不动**。测试 `tests/shelltitle.test.js`（12 条，含拿服务器上那本真书跑整条管线）。
  - **⚠️同一个函数的第二种坏法：epub 的「分册目录页」（2026-08-02 查《三体》第一部时发现）**。那一页正文就是一串章节名（`第一章 科学边界 / 第二章 台　球 / 第三章 射手和农场主 / 第四章 三体、周文王、长夜 / …`），`rbDeriveTitleAndBody` **把第一条揪走当了标题**，接着那趟「跳过重复的标题行」又吃掉了第二、三条，**吃到「第四章 三体、周文王、长夜」时因为标题里有个顿号、`READING_TITLE_RE` 不认才停下**。结果：目录页顶着「第一章 科学边界」这个名字排在真正的第一章前面（症状同《笑傲江湖》），而且页里前三条被永久吃掉。**这种不是空壳（604 字），上面那道「正文空了就不改名」的闸拦不住它**。**排查提示：目录里出现「同名两章、一个几百字一个几千字」，先去看那个短的正文是不是一串章节名。**
    - **当时判断「误伤风险大、先只改数据」是错的，第二天就打脸了（2026-08-03 补代码修）**：我在服务器上把标题改回 `三体I`，用户一拉，手机上又变成了「第五章 叶文洁 475 字」排在第一章前面。原因是 **`rbLoad` 的一次性标题修复按 `bk._titleFixVer` 判，而云端下回来的书身上没有这个标记**——每重新下载一次就重跑一遍、**每跑一遍再啃掉一口**（604 字 → 475 字；这次揪走的是第五章，因为第四章带顿号过不了正则）。**教训：只要坏在导入/修复管线上，改服务器数据是挡不住的，同步一次就打回原形，必须在代码里拦。**
    - 最终判据（`rbDeriveTitleAndBody` 开头）：**正文里像章节名的行 ≥3 条、且占非空行一半以上 → 整章原样返回**。落地前拿她库里 **367 本书全量扫过，只命中 2 章，都是真目录页**（另一本《肖申克的救赎》的第 1 章早被啃成了「第十章」，顺手改名成「目录」）。**⚠️别只卡「行数少」：真目录页有 35 行，比很多正常短章都长。** 回归见 `tests/shelltitle.test.js` D 组（含幂等、两条「别误伤」、真书 199 章零改动）。
  - **存量数据另修**：代码只防以后。服务器那份 51 章里有 4 个这样的空壳（第 1/14/26/38 章，正文 6~12 字），已按用户选择清掉 → 47 章、`ts` 更新，她打开书架靠 `bkSyncCheckUpdates` 自动更新。⚠️判空壳**两道闸都要**（正文 ≤30 字 **且** 每一非空行都是目录条目）：只卡长度会误删「正文只有一句话」的短章，只卡格式会误删短诗/短引文；且**必须逐行判**——第四卷那个壳是两行（`第四十回 曲谐` + `后记`）。改完核对过「保留的 47 章与原文逐字一致、只少了 4 个空壳共 31 字」。
- **⚠️「一部＝一章」的 epub 怎么切（2026-08-02 做《三体全集》）**：用户报「第二部目录很特别，没办法通过格式找出来」。实情是《黑暗森林》整个「上部」是**一个 xhtml 文件**＝一章 10.7 万字，《死神永生》第二~六部同样每部一章（3~10 万字）。**两部书两种解法，别混为一谈**：
  - **《死神永生》＝零猜测**：作者写了小标题，epub 里以 `【威慑纪元13年，审判】` 原样留在正文里。以这些行为界切开、标题就用【】里的原文，跟用户参考目录**逐条对上**（第二部 12 条 / 第三部 6 条 / 第四部 3 条 / 第五部 5 条 / 第六部 3 条）。**每部第一节沿用原章标题**（带「第X部」，平铺目录里一眼看得出进了哪一部）。「云天明的童话」那节 45,315 字**超过 `readerSummarizeChapterOnce` 的 40,000 字截断线**，内部按书里真实的「云天明的第X个故事：」+ 下一行故事名再切成三节。⚠️那个标题**分两行印**，而上一步已用 `'\n\n'` 重拼过正文，判「下一行」必须**跳过空行往后找**（第一版直接看 `lines[i+1]` 全落空、一刀没切）。
  - **《黑暗森林》＝作者根本没起过章节名**（豆瓣/微信读书/用户阅读 App 的目录都只到「上/中/下部」，搜索确认过）。**能用的只有原书排版的空行断点——正文里表现为「只含一个空格的行」（`l.strip()=='' and l!=''`）**，实测每个断点前后确是场景切换（航母工地→阿拉斯加雪原→张援朝退休→罗辑的地下室）。上部 48 / 中部 50 / 下部 40 个。名字只能「部名＋序号」（用户自己选的，另两个选项是「让 AI 逐节起名」——花额度且标题会剧透——和「不动」）。⚠️**碎片要并**：<600 字的节多半是排版残留，并进邻节；**开头那节太短要往后并**（那几个「危机纪年第X年」标记行自己占一节，前面没有节可并，第一版合并逻辑漏了这一条）。
  - **⚠️⚠️`rawText` 回拼时每块加「第N章 」前缀（这次新加的保护）**：`rawText` 只服务于目录里那个「重新拆分」，而这本书的新章名（`上部·面壁者 01`、`威慑纪元13年，审判`）**不匹配任何一条切章规则**——误点一下就会整本切成一坨、章名兜底成「前言/引言」，199 章的成果一次报销，**且服务器那份已被覆盖、无从恢复**。加前缀后 `chinese` 规则认得出，实测重切出 201 章（多的 2 刀来自正文里本来就有的像标题的行：获奖感言里提到「第三十六章 尾声·遗址」、目录页列着「第六部 银河纪元409年」），边界与现在基本一致。**误点的代价从「整本毁掉」降到「标题前面多四个字」。以后给这类「章名不可识别」的书改数据，都该这么兜一手。**
  - **改完必须逐字核对，别只看章数**：`re.sub(r'\s+','',全部正文)` 比对前后，差额必须**恰好等于**被提去当章名的那些原文（这次是 28 个【】共 525 字 + 3 组童话标题 43 字 = 568，一字不差）。另查「空正文的章」和「新增的重名章」。原始 60 章版本备份在 `/home/ubuntu/cc-books-backup/`。
  - **改的是服务器那份**（60 → 199 章 + 更新 `ts`），用户打开书架靠 `bkSyncCheckUpdates` 自动更新，不用手动删书重下。⚠️前提是**她这本还没读**（切章会让所有按「第几章」钉位的读痕/背景/问答错位）——动手前问过。
- **正文「改错字」（2026-07-28 加）**：用户原话「我现在上传的书籍，我希望能够编辑……我需要改里面的一些错字，和添加一些作者新出的番外」。此前唯一办法是删书重传，读痕/摘要/背景全部陪葬。**入口＝选中那几个字 → 底部划线小条上的「✏️ 改」**（`rdHlShowSelBar`）——改错字的第一步本来就是选中错字，手势天生对上，零新增手势零新增图标。⚠️**别改成「长按段落」**：手机上长按正文＝系统选词，而选词正是划线的入口（`selectionchange` → `rdHlShowSelBar`），两者会正面打架。
  - **⚠️三条铁律**（`tests/edit.test.js` 71 条全钉死了，改这块必跑）：
    ① **身份证 `fileName`/`fileSize` 一个字节都不许动**。读痕/划线/问答/背景/名单/书籍类型/文件夹/阅读进度/云同步全按 `fileKey` 认书，而它是**导书那天记下的标签、不是拿正文现算的哈希**（`rbBookFileKey` 就读那两个字段），所以只要不碰它，编辑正文对上面每一样都是透明的。照抄「改书名」的先例。旁证：书架界面根本不显示文件大小（本地书显示阅读进度、远程占位显示章数、字数现算），冻住一个跟真实内容对不上的数字**无处露馅**。
    ② **段落数量永远不变**。段号＝正文里第几个非空行（`readingModeRenderMerged` / `readingModeAddParaNums` / `_rdEditFindLine` 三处必须同一个数法）。所以编辑框**禁止换行**（回车＝保存、粘进来的换行转空格、`rdEditApply` 里还有最后一道闸），且不许把整段清空。一拆一删，这一章后面所有读痕/背景集体错位，跟「34 章的背景插进 33 章」同类，而且更难发现。
    ③ **四份一起改**：眼前 DOM／会话消息（`chatMessages`）／书架那本书（`book.chapters`）／云端（`bkSyncPush`）。只改会话＝书还是错的（重读、换设备照旧错）；只改书＝眼前不变。
  - **⚠️正文不在你点的那条消息里**：共读/阅读的正文渲染走 `readingModeGetOriginalText(msgIdx)`——从当前消息**往前找最近的 user 消息**，正文在那儿；点的那条（`.chat-msg.ai`）装的是点评/问答/背景块。而划线/书签/问答记的 msgIdx 都是 AI 那条。所以改正文前必须先 `_rdEditOrigMsgIdx(msgIdx)` 挪一格。**第一版直接改了 AI 那条 → 每次都报「找不到这一段」**，回归测试 C 组当场逮到。
  - **认章号**：阅读模式看 `msg.readerChapter`（100% 准）；**共读模式会话只记了书、没记章号**，靠 `_rdEditBookTarget` 用整章内容精确比对反查（`_rdEditNormText` 剥 `[Pn]`＋逐行 trim＋丢空行）。**在用户真实书库上实测：359 本 / 21624 章，只有 0.51% 的章节存在"另一章一字不差"**（且基本是切章切坏的碎片 `<br>`/`……`）。退路是按章标题唯一匹配——**17% 的书有重名章标题**，所以那条路写死了「重名就放弃」，宁可不改也不能改错章。都认不出时**只改会话并 sticky 提示「书没改」**，绝不瞎猜。
  - **⚠️共读消息带 `[Pn] ` 前缀，写回时必须还回去**（`_rdEditFindLine` 记下 `prefix`）——丢了整章段号解析全乱。
  - **划线跟着挪**（`_rdEditShiftHl`）：改动左边不动、右边平移 delta、压在改动上的撑开包住新字；`text`/`preview` 一并重算，否则读痕面板一直显示改之前的错字。**书签是段级的，不受段内改动影响。**
  - **后悔药**：`reading_book_edits = {fileKey:[{id,chap,title,p,start,before,after,ts}]}`，每本封顶 200，书架 ⋯「📝 改动记录」逐条 ↩️ 还原。⚠️**本地存、不进云同步**——正文本身已跟着书同步了，这张表只是操作痕迹；挂进 manifest 就得处理多设备并集（整块覆盖会把另一台的记录盖没，见下面同步心法），为一个后悔药付这个复杂度不值。还原前会核对「改后的字还在不在」，被二次改过就拒绝并说明。**书没改成（对不上章）就不记录**——记录挂在书上，无从还原。
  - **⚠️已知隐患（等第三块做完才消）**：跨设备自动更新**还没做**，所以别的设备会一直停在旧正文；而书架配置里那个手动「全上传」（`bkSyncPushAll`）会把该设备的旧正文推上云、冲掉改好的字。全局扫过：**没有任何自动/开机/定时的整书上传**（只有导新书、改字/还原、和这个手动按钮），所以不会自己倒灌，但那个按钮在旧设备上按下去就是事故。
  - 用户 2026-07-28 真机验过：「改了，没啥问题」。

- **追加章节 / 番外（2026-07-28 加）**：用户「添加一些作者新出的番外」。入口＝书架 ⋯ →「➕ 追加章节」→ 选 txt → 自动切章 → **预览确认**（列出每章标题、字数、接上去之后是第几章）→「接到最后」。`rbAppendStart`/`rbOnAppendPick`/`rbAppendSplit`/`rbAppendPreview`/`rbAppendConfirm`/`rbAppendChapters`，另起一个 `#rbAppendInput` 文件框（跟「添加书」分开，免得走串把番外导成新书）。
  - **⚠️只做「接到全书最后」，刻意不做「整本换新版」**（用户自己选的）：读痕/划线/问答/背景全按「第几章·第几段」钉位，接在最后＝前面每一章序号一动不动＝零风险；整本换新版要重新切章，章数或段数一旦对不上，前面所有标记**静默**集体错位，错了还不报错。
  - **⚠️`rbDedupChapters`/`rbFixChapterTitles` 只在「新来的这几章」内部跑**，跑完再 concat。前者会合并相邻同标题的章、后者会跨章挪副标题——放到整本上跑，万一动了已有章节边界就是上面那类静默错位。测试 C 组专门拿「新章跟已有末章同名」来守这条。
  - **⚠️`rawText` 必须一起接**：目录里的「重新拆分」(`rbReSplit`) 是拿 `rawText` 重切整本的，不接的话哪天点一下番外就没了。测试 B 组守这条。
  - **⚠️`fileName`/`fileSize` 一个字节不动**（同改错字的铁律①），`rbAppendChapters` 里还显式断言了一次。
  - **默认按这本书原来的切法切**（`b.preset`），预览里可以当场换切法就地重画。

- **跨设备自动更新（2026-07-28 加）**：改完错字/加完番外，别的设备原本不会跟着变——书是「本地为主、服务器当镜像」，`bkSyncInjectManifest` 里一句 `if (localKeys[fk]) return;` 就把已有正本的书整个跳过、压根没比时间。而服务器 manifest **早就带着 `ts`**（books.py 的 /manifest 就返回），牌是现成的。现在：本地书记 `_ts`（`bkSyncPush` 推成功时盖、`bkSyncDownload` 下载时盖），`bkSyncCheckUpdates(items)` 拉清单时比，服务器新就 `bkSyncRefreshOne` 重下正文并弹「《X》已更新（N → M 章）」。
  - **⚠️首次见面不下载、只盖时间戳**：老书本来没有 `_ts`，当成 0 的话一开书架就把几百本全重下一遍。所以第一次见到某本书认作"已同步"，**只有此后的改动才触发更新**。测试 F 组守这条。
  - **⚠️但「章数对不上」是例外，必须下（2026-07-28 补）**：用户问「改了字后怎么在别的地方同步呢」时发现的死角——她别的设备都还没"见过"任何书，第一次开书架只会盖时间戳，于是**服务器上刚批量修好的 90 本书永远同步不过去**，只能一本本手动删了重下。现在首次见面时先比 `m.nchap` vs `b.chapters.length`，不一样就下。`nchap` 是 manifest 现成字段、比对不花钱。⚠️**只认章数、不拿字数猜**：改错字不动章数，那种情况本来就该等下一次真更新，不值得为它把整个书库重下一遍。⚠️章数不对时**不能提前盖 `_ts`**（盖了就再也不会下了）——测试 F5 钉住。
  - **⚠️批量更新只报一个总数**：修完一批书可能几十本，一本一条 toast 会把屏幕刷爆。超过 3 本就 `quiet` 模式闷声下、末尾报「已更新 N 本书」；但**正在读的那本仍单独提示**（她眼前那一章得重开才看得到新内容）。测试 I3/I4 守这条。
  - **⚠️只换 `chapters`/`rawText`，保住本地 `id`**：`reading_conv_book`（哪个会话在读哪本书）、`rbActiveBookId`、阅读会话 id 全记着本地那个 `bk_<时间戳>_<随机>`，整个对象换掉会把这些关联全断。`fileName`/`fileSize` 同样不动。测试 H 组守这条。
  - **⚠️云端更旧时不下载**（别把新正文盖回旧的）；推成功才盖 `_ts`，否则会把自己刚推的当成"别人的更新"再下一遍。
  - 正在读这本书时提示「重开这一章看新内容」——正文快照是开章那刻拼的；同时作废 `_readingMergeCache`。
  - **⚠️遗留隐患**：书籍服务器配置里那个手动「全上传」(`bkSyncPushAll`) 在**还没更新过的**设备上按下去，仍会把旧正文推上云。自动更新只在打开书架（`bkSyncPullManifest`）时跑，所以"开一下书架再按"是安全的，但这个顺序没法强制。

- **⚠️收藏模型「明明选了却提示没选」＝中转站编号过期 + 静默失败（2026-07-28 修）**：用户在阅读模式从收藏里选模型，界面显示已选中，一点「讨论」却弹「请先选择模型」。她自己的描述直接指到病根：**「我选的是一个，但实际选中的有可能是另外一个」**。根因：收藏条目记的是中转站**内部编号 `providerId`**，而**删掉再重新加同一个中转站编号就变了**（空号）；`sel.value = 不存在的编号` 是 **DOM 的静默失败**——下拉框变成空白、不报错不抛异常。老 `chatPickFavModel` 切完不检查，后面照样写 `chatSelectedModel`、点亮发送键、把标签改成那个模型 → **界面显示已选好、实际中转站和密钥都是空的**，`readingAskOne`/`readingCommentOne` 那道 `if (!item || !chatSelectedModel)` 闸就报「没选模型」。
  - **⚠️提示语冤枉了模型**，所以她怎么重选模型都没用——现在 `chatNoModelMsg()` 分两种说：中转站没选中就点名中转站。**这类「A 坏了却报 B」的提示是最贵的**：她会顺着错误的方向反复试。
  - **修法**：`chatFavProviderId(fav)` 先按编号找、找不到**按 `fav.url` 兜底**（网址是中转站的稳定身份，改不了）——`chatResolveCompressModel` 早就是这个顺序，收藏这里当初漏了。**都找不到就提示「XX 已经不在了，去密钥页重新收藏一次」并直接 return，绝不假装选好**。
  - **⚠️顺手拆掉写死的 `setTimeout(…, 100)`**：老代码用它「等模型列表加载完再选中」，其实 `onChatProviderChange` 里动 `chatSelectedModel`/`chatSelectedKeyStr` 的几条路（`onChatKeyChange`/`chatLoadModelsForCurrentKey`）**全是同步的**，只有 `chatFetchModels` 异步且不碰选中的模型。赌网速的等待纯属隐患。
  - **⚠️⚠️但这不是她遇到的那个——用户当场否掉了「中转站过期」这个结论（「我觉得不是中转站的问题，应该是功能上的问题，它没有过期」），她是对的**：`chatRenderFavSection` 每次渲染**本来就会按 url 自愈 providerId**，而她正是从收藏区点的，编号早被修好了。**教训：找到一个「能解释症状」的 bug ≠ 找到她那个 bug；用户说「不是这个」时先信她、回去继续查。** 继续查出来的三个真问题（2026-07-28 同日修）：
    - **① 模型和中转站没有一起还原（最可能是她那个）**：`readerOpenChapter` 记下并还原 `chatSelectedModel`，**中转站的下拉框却一个字没管**；只要它在别处被清空过一次（`chatLoadConv` 里 `if (conv.providerId)` 为空时那整段还原被跳过），之后就是**模型一直有、中转站一直空**，怎么重选模型都没用。现在开章时中转站也一起还原（`_rconv.providerId → chat_last_provider`，只认下拉框里真有的那一项）。**铁律：模型/中转站/密钥这三样，谁都不许单独还原。**
    - **② 收藏区按「中转站名字」分组**：注释写着「不同站点绝不混排」，实际 `groups[fav.providerName]` ——**两个同名的中转站会被并进同一组**，她点的那一行其实属于另一个站（正是「实际选中的有可能是另外一个」）。改成按 `getBaseUrl(fav.url)` 分组，名字只当组标题显示。
    - **③ 收藏区标题的「当前:」会瞎猜**：精确匹配（站+模型）失败后**退回只按模型名找**，于是显示成**另一个站的同名模型**。改成只认精确匹配，宁可不显示「当前:」；中转站空了则显示「⚠️ 没选中转站」——**界面必须让「坏掉」看得见**。
    - **④ 自愈 `chatEnsureProvider()`**：讨论/点评/标背景/翻译四个入口在报错前先调它——中转站空了但模型还在时，按「这个模型属于哪个站」把中转站切回来并弹一句「已自动切回 XX」。⚠️**只在能唯一确定时才切**（收藏里有且只有一个站收藏了这个模型；否则退回 `chat_last_provider` 且它上次用的就是这个模型），**认不出就老实报错**——猜错＝请求发去别的站、扣别人的钱。用户自己选的「自动找回来 + 告诉我」。
  - **排查心法**：先在无头浏览器里**把她的症状复现出来再动手**（`tests/favmodel.test.js` 的 B 组就是那次复现脚本转成的）——这次一跑就看见「显示的模型=claude-y / 中转站=(空) / 弹没选模型=true」，比读十遍代码都快。
  - 测试 `tests/favmodel.test.js`（18 条）。

- **⚠️「每章都在重复」＝拆章把首行当章节名却没从正文删（2026-07-28 修）**：用户截图——章节名在正文开头又出现一遍，**每个长段落前面还躺着一条「它自己的前 80 字」**。根因是 `rbSplitByPattern` 的 `__EMPTY_LINE__` 分支：`lines[0].substring(0,80)` 当章节名、**整块（含首行）仍留在 body 里**；之后书在「回拼 rawText（`title + '\n\n' + body`）→ 重新拆分」转一圈，那条半截话就被**写死进正文**、再也拆不掉。**比对过 Readingo 原始 txt，源文件是干净的——是导入管线弄坏的**，别去怀疑书源。波及真实书库 **86 本 / 2206 处半截段 + 36 本 / 310 章标题重复**。
  - **三处一起改**：① `__EMPTY_LINE__` 分支——首行短(≤80)且后面还有正文就当章节名**并从 body 删掉**，首行本身就是一整段就老实叫「段落 N」，**绝不截 80 字**；② 所有回拼 rawText 的地方（`rbReSplit`/`rbAppendChapters`/`rbAddBook`/插入输入框那两处）统一走 `rbChapRaw(c)`——正文开头已经是章节名就不再拼一遍；③ 存量数据靠书架 ⋯「🧹 清理重复段落」（`rbDedupBook`）当场清，清完 `rbSave()`＋`bkSyncPush()`，并顺手问一句「书架上还有 N 本同样中招，一起清吗」。
  - **⚠️只删不增，且只认「正好 80 字 + 是下一段前缀」这一刀**：79 字的前缀、一模一样的两段都**不许动**——复读/回声是真实存在的写法，误删就是改坏原文。清理前在她真实书库上验过：清完的正文与原始 txt **逐段完全一致**。
  - **⚠️`rawText` 必须一起清**，否则她哪天点一下「重新拆分」，半截段原样长回来。
  - **⚠️只修服务器没用、会被手机盖回去（真踩过）**：书是「本地为主、服务器当镜像」，我在服务器上把《温柔故事》修好后**手机 40 秒后一推就覆盖了**，用户反馈「还是一样」。而且清理**不改章数**，`bkSyncCheckUpdates` 首次见面那条只比 `nchap`，别的设备也捡不到。**结论：这类存量数据订正必须做成 App 里的按钮，从正本改起。**
  - **⚠️清完正在读的那一章不会自己变干净**：共读会话里的正文是开章那刻拼进消息里的快照，改书改不到它——提示里必须写「重开这一章」。
  - 测试 `tests/dedup.test.js`（29 条）。

- **⚠️「改了书就要推服务器」——手动分章/重新拆分原本漏了（2026-07-28 补）**：`rbSplitOneChapter`（✂️ 手动分章）和 `rbReSplit`（重新拆分）改完 `b.chapters` **只调 `rbSave()` 存本地、没有 `bkSyncPush`**，服务器那份还是没拆的。后果：用户手动切好章、哪天删了重下，拿回来的又是没拆的那份，**白切一场**。用户自己问出来的（「我手动分好的，删除后再从服务器下载，下载下来的是分好的吗」），查了才发现答案是"不是"。现在改书的四处（改错字 `rdEditApply` / 追加章节 `rbAppendChapters` / 手动分章 / 重新拆分）**统一都推**，`tests/append.test.js` S 组（5 条）直接读函数源码断言里面有 `bkSyncPush(`，以后新加改书的功能忘了推会当场红。

- **⚠️加了「番外」这条切章规则（2026-07-28）**：用户追加番外时把标题写成 `番外一、`，切不出来——`cn-num-dot` 要求**行首第一个字就是中文数字**，前面多了「番外」两字就不认，整篇被切成一坨、兜底命名成「前言 / 引言」（她问「它为什么会自己取名叫前言呀？」）。她书库里 `番外·xxx` 有十几本，值得单列。**四处配套**：`RB_PRESETS.fanwai` ＋ `rbAutoSplit` 探测表 ＋ 目录的「切法」下拉（`#rbPresetSelect`）＋ 追加章节预览框的 `opts` 数组。
  - **⚠️正则两道闸都不能少**（写测试时当场踩到）：① 整行 ≤22 字；② **「番外」后面必须跟分隔符/序号或到此为止**。只卡长度的话「番外正文甲。」这种短句也会被当成章节名 → 真标题下面变空 → 被 `rbSplitByPattern` 末尾那句 `filter(c => c.body.length > 0)` **静默丢掉**，番外反而整个消失。（顺带记下：**标题行下面没有正文时，那一章会被无声丢弃**，这是老行为。）
  - **⚠️番外要「不管谁赢都能切出来」**：`rbAutoSplit` 是**只选一条得分最高的规则**，而最常见的形状是「正文几百章用第X章标、末尾两三篇用番外标」——番外永远赢不了，会被整个吞进最后一章。所以单独数一遍 `_fwCount`，命中就把番外那条**并进赢家规则**（`pattern + '|' + RB_PRESETS.fanwai`）；另外只有一两篇番外的文件（追加番外时最常见）原本会掉进「空行分隔」把每段当一章，现在也走番外规则。
  - **在她真实书库上验过**：正文里会被认成番外标题的共 **35 本 90 行，抽样全是真标题**（`番外一 求婚`／`番外·朱九篇`／`番外：降龙`／`番外5：道侣`），没有误伤。这 35 本重新拆分后番外能单独成章。**改的是切章规则，只影响以后导的书和「重新拆分」，不动已存好的书。**
  - **⚠️同一天补的另一半：光秃秃的「1.」「01.」原来也不认**（用户问「像那种 01. 1. 不会被认为是切分点吗，我有挺多是这样的」）。`numbered` 结尾写死 `.+`＝点号后面必须还有字，所以只有编号的一行匹配不上——**《温柔故事》整本被切成一坨、章名兜底成「前言 / 引言」就是这么来的**。改成 `^\d+[\.\、]\s*.+|^\d{1,3}[\.\、]\s*$`。**光秃秃那半截限死 1~3 位数字**，否则「2015.」这类年份行也会被当章节名。
    - **在她真实书库上量过再改**：24 本、871 行是这种写法；「望城」98 个标号只切出 4 章、「产虐」86 个只切出 2 章。新规则实测：望城 4→101 章、产虐 2→88 章、夜色溶溶 6→89 章、军旗 3→48 章、曲水流年 4→53 章。**epub 一本没中**（社科书脚注不长这样），无误伤。
    - **已知的老毛病（不是这次改的）**：`^\d+[\.\、]\s*.+` 会把 `2024.11.01` 这类**日期行**也当成章节名（「最讨厌的朋友.txt」里有）。改之前就这样，暂不动——那本书本来就是按日期分段的。
  - 测试 `tests/fanwai.test.js`（28 条，含番外 20 + 数字编号 8）。

- **⚠️中文章节正则漏了「〇」（2026-07-28 补）**：`RB_PRESETS.chinese`、`rbAutoSplit` 的探测表、`READING_TITLE_RE`、`_rbIsBareChapterNum` 四条都写的 `[…百千零两\d]`——**有「零」没有「〇」**，于是「第一三〇章」认不出来、整章被吞进上一章（做追加番外时截图当场逮到）。旁边的 `paren-num`/`cn-num-dot`/`cnNumParse` 早就带 `〇` 了，纯属当初漏字。**改的是切章规则，只影响以后导的书和「重新拆分」，不会动已存好的书**；扫过用户真实书库（359 本 / 21624 章）**当前 0 条受影响**，属预防性修补。

- **⚠️共读提问/点评是可并发的，别退回全局单锁（2026-07-30 改）**：用户报「阅读时如果有背景知识在生成、或共读提问在生成，就没法对其他段落双击，总要等它们生成完；翻译模式下就可以同时翻译不同段落」。**两个根因，只修一个照样会撞上另一个**：① 一把全局锁 `readingCommenting`，`readingOnParaClick` 第一行就把双击挡回去；② 正在生成的小气泡是**手工 `insertBefore` 进 DOM 的、不属于渲染数据**，标背景那种「边标边整条重渲」的循环一过，气泡连同刚弹出来的提问条一起被抹掉——**这才是「标背景时双击没用」的真因，跟锁无关**。改法**照抄翻译模式现成的 `translatePending`**：`readingPending` 每笔一条记录、各自带 AbortController，占位/流式气泡由 `readingModeRenderMerged`（新增可选第 3 参 `msgIdx`）**从数据里画出来**，重渲多少次都还在；`readingPendingHtml` 一套骨架给占位态和流式态共用（翻译那边是分两处写的，别照抄那半）。
  - **`readingCommenting` 保留成派生量**（`readingSyncBusy` 维护），`_waitIfStreaming` 那类旧调用方一字不用改——**别删这个变量**。
  - **⚠️记录存 `msg` 对象而不是 `msgIdx`**：往上滑补上一章会让所有下标整体挪位（同 `readerMarkBg` 那个「34 章的背景插进 33 章」的坑），渲染/收尾时一律现查 `chatMessages.indexOf(msg)`。`readingAskOne` 收尾原来直接用进函数时的 `msgIdx`，属同一类隐患，一并改掉了。
  - **提问条要在重渲后接回来**（`_rdActbarState` + `readingRestoreActbar`），连同打了一半的字；**接回来时不抢焦点**（`opts.noFocus`）——用户多半正在读，顶起键盘很烦。
  - **流式跟随改成「只在气泡已露在视口里才 `scrollIntoView('nearest')`」**：并发之后你可能问了 P10 又滑到 P30 去问，P10 出字时把你拽回去是最烦人的；`'nearest'` 对完全可见的元素是空操作，所以「正在看的那条边长边跟」照旧成立。
  - 同一段重复发由 `readingIsPending` 挡（**要等 guide 把 pNum 改写成小标题段号之后才判得准**），不白扣费；「停止」按钮只掐自己那一笔。
  - 测试 `tests/concurrent.test.js`（31 条）。⚠️**写这类测试要在每组开头清 `readingPending`**（`while (readingPending.length) readingRemovePending(readingPending[0].id)`）——上一组故意挂着不放行的请求会跨组累加，第一版就是这么假红的。

- **提问「带多少材料」三档小方块（2026-07-28 加）**：用户要自己决定每次带不带记忆表和摘要（「这个切换也走极简风」），选的形态是**一个小圆点、点一下换一档**，取代了原来那个「带前文问答」勾选框。⚠️**档位梯子的顺序不能改**：`0 只带原文 → 1 ＋记忆表和摘要 → 2 ＋本章前文问答`。这么排是因为**现状恰好落在 1 和 2 上**（表格摘要一直是 always on、前文问答才是可选的），换成「◐＝带问答、●＝全带」那套，「带表格但不带问答」这个现行默认就无处安放、会在用户没要求的情况下改掉她的行为和花费。存两个键：`reading_ask_lv`(普通提问，默认 1) / `reading_ask_lv_bg`(背景追问，默认 0)——**默认不同，合成一个键会打架**；老键 `reading_ask_ctx='1'` 迁移成 2。读取 `readingAskLv(isBg)`，`_lv>=1` 放行记忆表/摘要（**三条 `else if` 分支都要挡**，第三条 `readerEnsureSummaries` 会现场调 API），`_lv>=2` 放行 `priorQaBlock`。控件**用 CSS 画不用 ○◐● 字符**——半黑圆在部分安卓字体里会掉字，而它全靠形状表意。`readingAskCtxBoxHtml/readingBindCtxBox` 保留成薄壳转发，避免散着改漏调用点。测试 `tests/askcost.test.js` E 组（11 条）。

- **双击背景块＝背景追问模式（2026-07-28 加）**：用户报「双击的明明是背景知识，AI 却带入正文人物的动机跟我交流」。**根因**：`readingOnCommentClick(bqEl)` 只从块上取 `data-cp`（段号），**把「你双击的是哪一块」直接扔了**——于是双击背景块和双击正文段落发出去的东西**一模一样**：共读点评人设 ＋「结合该段原文认真回答」＋记忆表/摘要，三样都在往剧情上拽，那条背景讲解只是「你之前对 [P19] 写下的评论」里的一个小圆点。跑偏是必然，不是偶发。**修法**：渲染时给背景块打 `data-bg="1"`（`_cmtRaw` 去掉「汐：」后以 `BG_Q_PREFIX` 开头），`readingOnCommentClick` 把 `data-ci` 当 `opts.bgCi` 传下去 → `readingOpenAskBar`（按钮固定「追问」、不接长按第三态、空态只聚焦输入框）→ `readingAskOne` 的背景分支：人设换 `BG_NOVEL_PERSONA`/`SOCIAL_MODE_PERSONA` + 新的 `BG_FOLLOWUP_INSTRUCTION`，材料只有**那一条背景讲解 + 前后各三段原文**，**不带记忆表/摘要/同段其它问答**，记账单列成「背景追问」。实测 3558 → 1521 字（用户真机上普通提问是一万多）。⚠️**三条 `else if` 摘要分支都要挡**——第三条 `readerEnsureSummaries` 会**现场调 API 生成摘要**，漏挡等于白烧一笔它根本不需要的钱。⚠️**不是禁止聊剧情**：用户原话「不是说不可以聊正文」，定的是**背景优先、她问到剧情才聊**；写成硬禁令会从一个毛病换成另一个毛病（她顺口问「那他是不是在装傻」被拦回去）。⚠️**双击普通问答块那条路一字未改**，记忆表/摘要照带——「想认真聊剧情就双击正文」是这个设计的另一半，别顺手也给砍了。测试 `tests/askcost.test.js` D 组（15 条）。

- **共读提问的输入分块量表（2026-07-28 加）**：用户问「共读提问一次上万 token、主要是输入，怎么优化」。总数看得见、构成看不见，砍哪一块全靠猜——而且**记忆表格和前文问答有多大只有用户手机上的数据知道**，服务器这边测不出来。所以定的是「先量再砍」：`readingAskOne` 在发请求时算一份 `_parts`（各块**字数**，不是 token——token 只有整笔的真实值、拆不到块，比例够用来做决定），经 `chatStreamChat(opts.parts)` → `apiLogRecord(..., parts)` 落进明细日志，用量面板里那一条**点开就是一排条形图**（`_apiPartsHtml` 按字数倒序、`apiLogToggleParts` 就地切显示——⚠️不能整块重渲，`apiLogRenderBody` 会把展开状态冲掉）。**只记共读提问**（用户明确说社科导读没必要）。⚠️**记忆表和摘要必须分开记**（2026-07-28 拆）——最初合成一块「记忆表+摘要」，用户在真机上量出**这一块 6700 字 / 总共一万多**，正要决定砍哪个，而量表恰好在最该分辨的地方糊住了，等于没量。**教训：分块宁可拆细别合并**，合并的代价是整轮白量。**服务器实测靠不住**：造的假书里原文占 81~88%，而用户真实数据里原文只占三成——记忆表和摘要是全书累积的，服务器这边没有那份数据。⚠️**新增输入块必须同步加进 `_parts`**——`tests/askcost.test.js` 有一条断言核对「分块合计≈真正发出去的字数（误差<5%）」，漏记一块就会红，因为漏记会让用户按错误的比例去砍、白折腾一轮还以为是自己判断错了。**下一步待定**：`CTX_CHAR_BUDGET=8000`（`readingAskOne` 里）是最大的一块，等用户看过真实占比再决定砍到多少。**已排除的方向**：① 记忆表和摘要是绑在一起发的（同一条 system 消息、一个 if/else 链），曾想把 ◐ 拆成两档分别控制——用户 2026-07-28 明确说「**5 段前情我可以接受**」，**别再提砍 `CHAT_SUMMARY_MAX_SEGS` 或拆档**；② 社科导读不记分块（用户说没必要）。

- **右滑返回手势（2026-07-29 抽成公用）**：`attachSwipeBack(el, canStart, onBack)` —— **从屏幕左半屏起手、往右划够 80px** 触发，左边缘冒一条主题色提示。聊天面板（对话→列表→退回标签页）和书架面板（**目录 → 书架(文件夹内) → 文件夹列表 → 关掉**，用户要求文件夹也算一层）共用这一个函数，手感参数只有一处。⚠️`dx<0` 或竖向偏移 >60px 立刻放弃——**误触发（滚正文时面板自己关了）比"滑不动"贵得多**，`tests/swipeback.test.js` 的 B 组专钉这条。⚠️判「在不在目录里」看 `#rbChapterView` 的 display、**不看 `rbActiveBookId`**：远程占位书下载中/失败时 id 已有值但界面还在目录。⚠️提示条是全局共用的一个懒创建节点，别改成每个面板各建一个。**写这类测试要先造两本假书**：`rbRenderShelf` 在书架为空时会把 `rbCurrentFolder` 重置成 null，空书架下文件夹那层根本不存在，会假阳性地"少退一层"。
- **三模式选择器**：`对话 / 翻译 / 共读`（无 emoji），分段按钮，互斥。`chatSetMode()` / `chatSyncModeButtons()`。默认打开固定显示**密钥页**（`init` 里 `savedTab='keys'`，不再恢复上次标签）。
- **上下文压缩**（`chatCompressContext`）：对话模式默认 40 条触发、保留 20（设置里「触发」滑块可调）；**翻译/共读固定 10 条触发、保留最近 5 条**（写死保证一致）。滑块在翻译/共读下禁用、显示「10 条（自动）」。
- **摘要（断点续传设计）**：分段存 `chatCompressSummaries`，`chatCompressedCount` 逐段推进；某章总结失败（网络/回空）**就地停住不推进计数**，那章连同后面留作未压缩、下一轮共读自动补上（绝不越过、否则永久跳过）。可在「📋 摘要」弹窗编辑/删除/**♻️单段重新生成**（按 `chatCompressSegEnds` 记的原文范围重切；旧版无范围记录则提示手动补）。记忆表格缺回可「补齐/重建」（只用现有摘要重折、不重读正文）。保存用 `chatSaveCurrentConv(true)`（**没有 `chatSaveConv` 这个函数**；`true` 强制标脏，否则自动生成的摘要不会同步）。
- **翻译剧集记忆（2026-07-16 整轮重构，最终形态）**：`chatMemoryTables` 挂 4 个数据键——**三张展示表** `characters`(人物)/`places`(地点)/`cases`(**文化背景**，就是原「案件/剧情线」改名，key 没动) + **前情** `recap`。`terms`(译名)/`settings`(设定) 两表已废弃、不展示不提取，旧数据在提取重建时**原样保留不销毁**（可反悔）。列定义 `_mtTransCharCols`/`_mtTransPlaceCols=['地点','说明']`/`_mtTransCaseCols=['线索/事件','进展','关键点','状态']`。
  - **来源分工（关键）**：人物+地点 ← **前情自动提取**；文化背景 ← **追问/记住**。① 追问引擎 `chatTransHarvestMemory` 现只更 `cases`、**就地改不重建对象**（别再整体重建，那会抹掉别的表——recap 就被抹空过一次）。② `chatTransMineCharPlace(recapText)` 从一段前情梗概增量提人物+地点，就地改 characters/places；`chatTransUpdateRecap` 每压完一集自动调它、给该 recap 段打 `_mined`。兜底 `chatTransMineMissing`(找没 `_mined` 的段)/`chatTransMineAll`(逐集补，人物/地点面板顶「🔄 从前情补齐」按钮)。
  - **前情 recap**：数组 `[{text,srcIdx,_mined}]`（旧单块字符串按需迁移，`srcIdx:null` 排最前）。每集一段 `chatTransUpdateRecap(src,manual,srcIdx)`（≈300字/`max_tokens:1100`，按 srcIdx 去重、加完 sort）；自动压上一集 `chatTransUpdateRecapFromPrev`（`_recapped` 防重）、手动「压缩本集」`chatTransRecapCurrent`、批量「补齐前 N 集」`chatTransRecapAll`。显示复用摘要段 `_segHtml`（折叠+字数+🗑删+♻️重做）。
  - **⚠️前情不再喂 AI**（用户 2026-07-16 选省额度）：`chatTransRecapJoin` 已从追问 `translateAskRun` 移除。跨集信息全靠三张表（`chatTransMemoryPromptBlock` 回喂人物/地点/文化背景）；前情只作「给人读 + 提人物地点的原料」。本集内语境靠 `translateAskRun` 里 userMsg 的「本集前文」（预算 `CTX_CHAR_BUDGET=200000`，用户选保持整段）。
  - **人物/地点提取＝增量省 token（2026-07-19）**：`chatTransMineCharPlace(recapText,signal)` 现让模型只吐「新增/有变化的行」（`max_tokens:2000`），客户端 `_transUpsertRows` 按名字 upsert 合并（非空字段覆盖、空字段留旧、旧行不动）；给动过的行打 `_chg='new'`(绿点)/`'upd'`+`_prev`(橙点)，每次提取先清上批标记。「🔄 从前情补齐」`chatTransMineAll` 有停止键（`chatTransMineStop`：`chatTransMineAbort`+`chatTransMineCtl.abort()`）；`chatStreamChatBigOutput` 遇 AbortError 不再降档重试。
  - 测试 scratchpad：`test_recap_segs/backfill/render.js`、`test_char_place_from_recap.js`、`test_three_tabs_render.js`、`test_ask_no_recap.js`、`test_mine_delta/dots/stop/btn.js`、`test_askbar.js`（playwright 在旧 scratchpad `48f02160.../node_modules`，`NODE_PATH=` 指过去跑）。
- **共读/阅读记忆表格＝增量+删除省 token（2026-07-19）**：四张表（人物/地点/年表/主线）的**自动增量**更新（共读 `_updateMemoryTables`、阅读 `readerUpdateMemoryTables` 的非 rebuild 分支）改用 `_MT_SYS_PROMPT_DELTA`——模型只吐「增改行 + 【删除】清单」（`max_tokens:50000` 保持高位——省 token 靠模型少吐、不靠改上限；上限调低反而会截断首次建表/大批量的后半张表），共享引擎 `_mtApplyDelta(result,tables)` 就地并进：`_mtUpsertRows`(按 `_mtRowKey` 主键 upsert+打绿/橙点) + `_mtDeleteByKeys`(删除清单显式删行，人物/地点按名、年表/主线按事件) + `_mtParseDeletions`(解析【删除】小节)。⚠️**删除必须显式**（增量里「省略」＝没变，不再等于删除）。⚠️**「本批无更新」是正常态**，只要 `_mtDeltaLooksValid(result)` 就推进水位/upTo（否则会永远重喂）；真·空/乱码才不推进、下轮重试。**手动「补齐/重建」(`chatRebuildMemTables`/`readerUpdateMemoryTables(rebuild=true)`) 仍走整表 `_MT_SYS_PROMPT`**（权威重折，需要吐全表）。测试 `test_mt_delta.js`。
- **名单（人名/地名高亮）云同步（2026-07-27 加）**：用户报「设置里名词的名单好像不太会同步」——**不是偶发，是压根没进同步**：`reading_names_*` 从来没挂进 `cfBuildManifest`。而且**光挂上去也没用**：它原本按 `readerBookId` 存，而书 id 是 `bk_<时间戳>_<随机>`、**每台设备导入时各生成一个**，同一本书在手机和电脑上 id 不同，同步过去对不上号。所以键改成跨设备稳定的 **fileKey(`书名|字节数`)**（书签/划线早有先例：`readingMarksConvId()` 就是干这个的）。配套三件：① `_readingNamesMigrate(newKey)` 打开某本书时把旧键（书 id / `reader_书id`）搬过来并删旧；② `_readingNamesSweep()` 打包前全量扫一遍，把**没打开过的书**也搬了，否则推上云全是死键（幂等，书没加载完会重置标志等下次）；③ 落盘统一走 `_readingNamesWrite`（打 `reading_names_ts` + 3 秒防抖推云）。同步语义照抄书名/书籍类型：**整块按时间戳后写覆盖**（`readingNamesApplyAll` 先清本地所有名单键再写云端那份——不清就成了只增不删，别的设备删掉的名字会在这台机器复活）。⚠️`reading_names_on`(高亮总开关) 和 `reading_names_ts` 前缀一样，打包和清理时**都必须排掉**。测试 `tests/names.test.js`（18 条）。
- **书籍类型：小说 / 社科（2026-07-27 加）**：整个阅读体系原本**只为小说造**（全文 22 处「小说」硬编码：防剧透、伏笔、人物表、剧情摘要）。现在每本书带类型，`reading_book_kind = {"文件名|大小":"social"}`（照抄 `reading_book_titles` 那套：localStorage + `_ts` + **必须挂进 `cfBuildManifest`**，另加拉取端按 ts 覆盖 + 备份导入端），**默认（表里没记）＝小说**，书架 ⋯ 菜单「🎓 标为社科书」手动标。判定入口 `rbIsSocial(b)` / `rbCurBookIsSocial()`（阅读模式看 `readerBookId`、共读看 `rbGetConvBook()`）。**社科书的分岔点**：① 点段操作栏多「导读」键；② 人设换 `SOCIAL_MODE_PERSONA`（**刻意不读系统提示词框**——框里是读小说的蘑菇人设，含男生子题材声明 228 字 + 家族树画法 186 字，对社科书是噪音，且「像朋友唠嗑不是赏析课」跟"讲清楚"反着来）；③ 摘要/全书脉络改论点版；④ `readerUpdateMemoryTables` 直接 return false（没人物没伏笔）。改这块**务必同时改小说分支**、并跑回归确认小说路径一字未变。
- **社科书「导读」（2026-07-27 加）**：解决「读社科书要手打『请对以上做导读』+ 手打小标题当路标」。**小标题自动识别**：`rbLineIsHead(s,nxt)`（2~24字、不以句末标点收尾、非 ●列表/`a.`选项/图注、后面须跟 ≥40 字正文段）+ `rbDetectHeads(lines)` + `rbSectionOf(lines,pNum)`→`{headP,startP,endP,title}` + `rbSectionText`（`SECTION_CHAR_BUDGET=12000`，超了从**节尾往前**留）。在《社会性动物》9 章正文实测 **67 个小标题全中、零误判**。**生成走 `readingAskOne(msgIdx, pNum, '导读', bar, {guide:true})`**——第 5 参 `opts.guide` 是唯一新入口，函数内把「你点的段」扩成整节、**把 pNum 改写成 `sec.headP`**（于是流式锚点/落盘/`readingRerenderMsg` 全部自动挂到小标题后面＝插节首，重读时先看导读），其余流式气泡/停止键/`reader_qa` 同步/读痕**全部复用不另起一套**。用户定的形态：**只要人话讲解**（不加概念块/论证脉络块）、允许引书外例子和后文关联但以本节为主、**插节首不跳回**。测试 `sec.js`/`guide.js`/`regress.js`。
- **阅读模式「标背景」（2026-07-27 加）**：解决「读非虚构缺的不是生词、是背景」——字都认识，但不知道那个年代/制度/学派是怎么回事，而且**往往不知道自己缺**，所以做成系统主动标、不是等她想起来问。入口在**目录**：阅读分支每行统一一个「⋯」（跟书架的书一致），菜单＝`🏛 标背景 / ✂️ 手动分章 / 🗑️ 清掉本章背景`；**✂️ 从行上收进了菜单**（长章原本比别人多一个图标，收进去反而齐整），meta 行显示 `· 🏛 已标 N 条`，并**去掉了行尾的「阅读 ›」**（用户嫌多余）。**共读分支的目录一字未动**（仍是 ✂️＋＋，无 ⋯）。
  - **两遍法（别改回一遍）**：一遍到底＝把整章丢给模型让它自己看着办，那正是共读「散漫点评」的形状——用户原话「散漫地点评，模型注意力一般」，前几段挑得细、后面就敷衍。所以 ① `_rdBgScan` 用**便宜的压缩模型**只列清单（`P段号|背景点名`，输出短＝注意力不被拉长），超长章按 `BG_SCAN_CHUNK=20000` 切段分别扫；② `_rdBgExplain` 用**主模型**逐条讲，每次只喂这一个点 + 前后一小圈上下文。
  - **⚠️模型必须在 `readerOpenChapter` 之前定死**：它调 `chatResetConvState()` 会把 `chatSelectedModel`/`chatSelectedKeyStr` **清空**，之后要等 `chatLoadModelsForCurrentKey` 异步恢复。`readerMarkBg` 开章前就算好 `explainM` 传进 `_rdBgExplain`——现读全局的话，从目录标一个没打开的章节会拿着**空模型名**发请求（实测踩到过）。
  - **零新增存储键**：一个背景块＝一条 q 以 `BG_Q_PREFIX='背景：'` 开头的 `reader_qa` 问答。云同步、换设备重灌、长按删除/重新回答、读痕面板**全部白拿**。跨章去重靠 `readerBgDoneTerms(fileKey)` 扫全书已讲过的词、塞进第一遍的「已经讲过、请跳过」。**⚠️比对必须走 `_rdBgKey(t)` 归一化**（2026-07-27 加）：原来是一模一样的字符串比对，模型这一章写「共济会」、下一章写「共济会（Freemasonry）」就成了两个词，用户被讲第二遍还扣一次费。归一化抹掉括号注解/书名号引号/空格中点连字符/英文大小写/全角字母数字，**只用于比对，显示仍用模型给的原样**。⚠️故意**不做包含关系**（「独立战争」⊂「美国独立战争」）——那会把「宪法」和「美国宪法」这种真该分开讲的也无声吞掉。注意：喂给模型的「请跳过」名单仍是原词（给人和模型看的），硬过滤在客户端。
  - **⚠️车轱辘话：同一段被拆成三四条讲同一件事**（2026-07-28 修，用户实测《大唐辟珠记》第 44 章）。第 19 段同时出了「高门女子悬丝诊脉与帷幔隔诊」「帷幔隔诊」「丝帕覆腕诊脉是高门女眷就医礼制」「悬丝诊脉与帷幔隔诊礼制」四条——**四次扣费、四个块堆在同一段下面**。`_rdBgKey` 只抓「同一个词的不同写法」，抓不住「同一件事的不同说法」。三道闸：① 提示词要求背景点名称是**短名词（2～8 字、词典词条那样）**，明令禁止「…是…」「…指…」这种解释性尾巴，并写死「同一段最多只列 1 条」；② `_rdBgNear(a,b)` 近义判定＝包含关系 ∪ 字符重合率≥50%，两者都要求短的一方≥4 字；③ **每段额度按段落长度给**：`_rdBgCapOf(line)` ＝ `1 + floor(字数/200)`、封顶 `BG_MAX_PER_PARA=3`。⚠️**别写死 1**（用户 2026-07-28 提醒）——小说段落短、一段一件事，1 条正好；自传/社会学的段落动不动三五百字，里面真有好几件互不相干的事，写死 1 条就是漏。**故意不按书籍类型分岔**：段落长度本身就是更准的信号（社科书的段落天然就长），而小说里偶尔一段长描写多给一两条也不冤。**⚠️`_rdBgNear` 只许在同一段之内用，别顺手推广到全章**——跨段的「宪法」/「美国宪法」、「独立战争」/「美国独立战争」是真该分开讲的（C2 组测试守着这条线，推广过去会当场变红）。重标同一章时靠 `_rdBgScan` 的第 7 个参数 `donePara`（`readerBgOfChap` 出来的 `{p,term}`）把段号带进来，否则「这段已经有一条了」无从判断，再标一轮又叠一层。
    - **⚠️⚠️这三道闸里，第①和第③收过头了，2026-08-03 放宽（别再往回改）**：用户读一篇讲司马迁的文章时报「**一段里其实会出现不少要解释的背景点，但模型通常只解释一个，这就导致我还需要二次提问**」，并且**自己指到了病根**——「可能跟我之前担心一段会有重复的背景点、设了限制有关」。她是对的：**要治的从来是「同一件事说四遍」，不是「一段里事情多」**。真正对症的是闸①的「名字必须是短名词、不许带解释性尾巴」和闸② `_rdBgNear`（它们管"重不重复"）；而「同一段最多 1 条」管的是"多不多"，**拿数量去治重复，代价就是把真有好几件事的密集段落砍掉**——历史类文章一段里「太史令」「腐刑」「李陵之祸」「天汉三年」是四件互不相干的事，按老公式 150 字只准 1 条，必然漏三件，她只能一条条二次提问。
    - 具体改动：提示词里「**同一段一般只列 1 条**」→「同一件事只列一条 + **一段里有几件互不相干的事就列几条，别为了少列而漏掉**」（并举了官职/刑罚/事件/年号那个例子）；`BG_PARA_CHARS_PER_PICK` 200→**120**、`BG_MAX_PER_PARA` 3→**4**。闸②近义去重原样不动，它才是防重复的那道。回归 `tests/bg.test.js` N 组（新增「密集段四条都留得住」，用的就是司马迁那段的形状）。
  - **边标边插必须包 `_rdKeepScroll(msgIdx, fn)`**：插入点在视口上方会把正在读的那一行顶跑（实测不锚定会从 P22 窜到 P21）。它记下视口顶部那段的位移、重渲后用差值补 `scrollTop`。**别直接 `readingRerenderMsg(mi, focusP)`**——带 focusP 会 scrollIntoView 把用户拽走。
  - **⚠️再点＝「接着标」，不是重标**（2026-07-27 当天改）：初版是「本章标过 N 条，清掉重标吗？」——用户实测跑到第 4 条就失败，于是只能在「留着残缺的四条」和「全推倒重来」之间二选一。现在不清空，已讲过的词由 `readerBgDoneTerms` 收进第一遍的「请跳过」名单，天然接得上；真想重来走菜单里的 `🗑️ 清掉本章背景`。
  - **⚠️单条失败不许拖垮整轮**（同上）：`_rdBgExplain` 每条重试一次（间隔 1.5s），仍失败就 `failed++` 跳过、接着下一条；只有连清单都没拿到才算整轮失败。中转站偶发 429/超时很常见，为此把后面十几条全废掉太亏。收尾 toast 会报「已标 X 条，Y 条没成功：<错误>（再点一次「接着标」会补上）」。已插进去的每一条都是**即时落盘**的，任何失败都不会丢。
  - **⚠️开章会把模型选择清空——已在 `readerOpenChapter` 里修（2026-07-27）**：`chatResetConvState()` 清 `chatSelectedModel`/`chatSelectedKeyStr`，而 `readerOpenChapter` 不像 `chatLoadConv` 那样从 conv 恢复。后果是**从目录点一章之后，导读/共读提问/标背景全弹「请先选择模型」**；更坑的是它随后 `chatSaveCurrentConv` 把 `model:''` 写回这本书的会话，下次载入照样空——**不是一次性的，会一直烂着**（用户报「明明选好了模型，每次点接着标都让我重选」）。修法：重置前存下旧值，重置后按 `旧值 → readerFindConv(bookId).model → chat_last_model_<provId>` 还原，并同步 `chatModelInput`/`chatModelLabel`/发送键。
  - **⚠️`chatStreamChat` 没有任何超时**：中转站卡住不返回时 `await reader.read()` 永远挂着。标背景是十几条的循环，一条挂死就整轮不动、还把 `readerBgRunning` 焊死在 true（用户报「很久都不响应」）。所以走 `_rdBgTimed(opts, ms, who)`——每条自带超时闹钟（找点 120s / 讲解 90s），**超时抛普通 Error（可重试/可跳过），用户按停止才抛 AbortError（整轮结束）**，两者必须区分开。运行中再点菜单会 `confirm('要停下吗')` 而不是闷不吭声地挡回去。
  - **⚠️超时提示必须点名是「哪个模型」没回（2026-07-27 修）**：两遍走的是**两个不同模型**（第一遍摘要模型、第二遍主模型），旧文案只说「中转站没回」，用户直奔聊天那个中转站去查、那边好好的，于是以为不是模型的事（原话「说是中转站问题，可是中转站没问题呀」）。`_rdMdlName(m)` → `模型名@域名`，由调用方传给 `_rdBgTimed` 的第 3 参 `who`。
  - **⚠️第一遍 `max_tokens` 必须给足（4000，别调回 800）**：`chatStreamChat` **只收 `delta.content`，思考型模型的推理过程不算在内**。800 对不思考的模型绰绰有余（清单就二十来行），但思考模型光"想"就把额度用光、正文一个字都吐不出来 → `out=''` → 0 条 → 每次都报「这一章没有新的背景要补」，而两万字输入和推理 token 照样计费。用户实测连点三轮、烧了额度、一条讲解都没有。按实际生成量计费，抬上限对不思考的模型几乎不增加成本。
  - **⚠️第二遍 `_rdBgExplain` 是同一个坑的第二处（2026-07-27 修，当天先只修了第一遍、漏了这里）**：`max_tokens` 原为 **700**——讲解本身才 120 字（约 200 token），剩 500 给思考**正好卡在边界上**，于是「第 1 条插进去了，后面全没了」。空回复走 `if (!txt) { failed++; continue; }`，**请求发了、钱扣了（还白重试一遍）、却什么都没插**，收尾提示只有「N 条没成功：」后面空空如也。已改 4000，并让**空回复抛描述性错误**而不是返回空串——否则 `lastErr` 是空的，用户完全看不出为什么。**改这类上限时两遍必须一起改。**
  - **⚠️⚠️别把 msgIdx 揣在兜里跨过 await（2026-07-28 修，这是「9 条只成 1 条」的真凶）**：标背景一轮要发十几个请求、几十秒，而用户还在读——只要往上滑到章首，`readerPrependPrevChapter()` 就把上一章插到数组最前，**所有消息下标整体 +2**（那函数自己也要把已渲染节点 `data-idx` 和书签的 msgIdx 一起 +2，可见早有先例）。于是循环里捕获的 `msgIdx` 指到上一章头上：用户实测「三十四章的背景插进了三十三章」。**同一个根因也解释了之前那次「生成 9 条、读痕只剩 1 条」**——后 8 条没丢，是写进上一章去了；踩不踩到全看那一轮有没有往上滑，所以时好时坏、换不换模型都可能复现（我一度误判成模型问题）。修法：**捕获消息对象 `msgObj`**（对象引用不会因插队失效），内容写 `msgObj.content`；DOM/落盘要的下标每条现查 `chatMessages.indexOf(msgObj)`，查不到＝这一章被卸载了，当场停下并提示重开再「接着标」，绝不把内容写到别处。⚠️`readingAskOne` 早就是持对象的写法，改这类循环时照它抄。回归测试用「讲完第 1 条就插一章进数组」来复现（把修法改回去会立刻红 4 条）。
  - **进度条点开＝这一轮的背景点清单（2026-07-28 加）**：用户原话「不然我总要等到全部生成完才知道有哪些」——清单第一遍就扫出来了，没理由捂到最后。`_rdBgPicksSet(picks)` 在扫描一返回就填，`_rdBgPicksMark(k,st)` 在循环里推进（`wait 待讲 / run ⏳ / ok ✓ / fail ✗`）。点 `#rdBgBar` 本体展开 `#rdBgList`（另起一个 fixed 元素、钉在条子正下方，**不做成子元素**——条子是 `width:fit-content` 的胶囊，塞进去会把胶囊撑变形）。⚠️「停止」键要 `stopPropagation`，否则想停却把清单展开了；⚠️`_rdBgListPlace()` 要挂进 `_rdBgBarPlace()`，条子随滚动移位时清单得跟着走；⚠️`_rdBgBarHide` 要连清单一起收、并清空 `_rdBgPicks`。
  - **⚠️间歇性失败：退避重试 + 条间限速 + 跑的时候别推云（2026-07-28 加）**：用户实测《大唐辟珠记》9 条只成第 1 条，后来**没换模型再跑一遍却全成了**——所以不是某个模型固定坏，是**间歇性**的。「第 1 条成、后面连挂」正是"连着猛发"的形状：这个循环原本背靠背发请求，一秒能打出去七八个，很容易撞中转站频率限制。三处一起改：① 重试 1 次固定等 1.5s → **3 次退避 0/2s/6s**，错误里带 429/rate/too many/频率 再至少等 8s；② **每条之间 sleep 500ms**（9 条只多 4 秒）；③ `_readingNamesWrite` 的推云防抖遇到 `readerBgRunning` **一直往后顺延**——那一轮每插一条就并一次名单，3 秒防抖必然在中途触发，等于在十几个模型请求当间插一次整包上传、跟它们抢手机的网。⚠️这三条都是**减少诱因**，不是确诊；真正的诊断靠 sticky 失败提示里的原因文字。
  - **⚠️失败提示必须 sticky + 说清空回复的两种成因（2026-07-28 加）**：收尾那句 `showToast(...)` 原来**没传第 2 参**，2 秒就自己消失——而它恰恰是「钱花了却什么都没拿到」时唯一的解释。用户实测《大唐辟珠记》9 条只成 1 条、其余全白扣费，回头只记得"扣费了、只插进去一条"，完全不知道败在哪。现在失败一律 sticky（成功的照旧自动消失）。同时 `chatStreamChat` 增加 `_lastThinkLen`（**只数不收** `delta.reasoning_content/reasoning/thinking` 的长度，别拼进 `full`），`_rdBgExplain` 据此把空回复分成两种：**「只吐了思考过程（约 N 字）」→ 换个不思考的讲解模型**；**「一个字都没回」→ 中转站/模型本身的问题**。两者处置完全不同，含糊一句话会害用户白点好几轮。
  - **⚠️空清单必须分四种报（2026-07-27 加）**：`_rdBgScan` 返回 **`{picks, replies, matched}`**（`replies`=各段原始回复，`matched`=正则认出的行数，含随后被滤掉的）。调用方据此分开：`matched>0`→「这一章的背景点都已经讲过了」（模型照做了，只是词都被 `doneTerms` 滤掉——**初版漏了这条，会误报成「没按格式回」冤枉模型**，实测踩到）；`replies` 全空→「模型一个字都没回（多半是 max_tokens 被思考过程吃光了）」；`^无$`→「这一章没有新的背景要补」；其余→「模型没按格式回，认不出清单：<前 40 字>」。**混为一谈的代价是真金白银**：用户以为「这章确实没背景」，又点两轮想让它接着标，每轮都把整章完整喂出去一遍。
  - **⚠️渲染会静默丢弃段号越界的块**：`readingModeRenderMerged` 的 `for (j < pHtml.length)` 只输出 1…段落数 之内的 `commentMap[pNum]`，超出的**既不报错也不进章末兜底（那个只收 p≤0），凭空消失**；而 `readerPersistQA` 存的时候不检查范围。所以「reader_qa 里有、正文里没有、重开还是没有」是可能的。排查「生成了但不插入」时，先用**读痕面板**（直接读 reader_qa）对一下条数，就能区分「没存进去」和「存了没渲染」。
  - **清单解析要容错**：只认严格的 `P7|共济会` 时，模型稍加列表符号/编号/加粗/中文冒号就整轮解析不出东西。段号越界、重复词仍照旧丢弃。
    - **⚠️2026-08-03 又放宽了一大截（用户报「提示模型没按格式回，试了两次都失败」）**：**第一遍走的是摘要模型**（`chatResolveCompressModel`），那多半是个便宜的小模型，而小模型最不擅长的就是死守格式。老正则要求**必须有 P 前缀 + 必须是 `|`/`：` 分隔**，于是这些只差一点点的写法一条都不认：`7|共济会`（吞掉 P）／`第7段|共济会`／`[P7] 共济会`／`P7 共济会`（空格当分隔）／`P7-共济会`／markdown 表格 `| P7 | 共济会 |`。**整轮 0 条，而整章两万字已经喂出去、钱照扣**。现独立成 `_rdBgParseLine(ln)`，上面全部认得出。
    - **⚠️宽是有边界的，两道闸**：① 段号前面既没 `P` 也没「第」时，**必须**有明确分隔符（`|｜:：、`）——否则正文里「1958 年大跃进那阵子」会被读成 P1958；② 用空格/短横线当分隔时，名字要 **≤20 字且不带句读**——否则「P7 这一段讲的是共济会，它……」整句会被当成背景点名称。
    - **⚠️写头部正则时别把段号后面的空格一起吃掉**：`P7 共济会` 正是靠那个空格分隔的，`\s*` 吃掉后就变成「段号顶着正文」反而认不出。头部只匹配到数字（和紧跟的「段/章」）为止，剩下的原样交给分隔符那一步。第一版就踩了，测试当场逮到。
    - 回归 `tests/bgparse.test.js`（A 组＝老格式一条不丢／B 组＝新认的走样／C 组＝该拒的都拒）。
  - **⚠️「没按格式回」这句提示本身也修过（2026-08-03）**：① **没点名是哪个模型**——第一遍是摘要模型、第二遍才是主模型，旧文案只说「模型」，用户会直奔聊天那个模型去查、那边好好的（跟超时提示当初那个坑一模一样）；② **只给回复的前 40 个字**，而那 40 个字恰恰是唯一的诊断材料，截掉等于没报。现在 toast 里点名 `_rdMdlName(scanM)`，全文另开 alert 给她看，并直接给处置：「去设置里把摘要模型换一个」。③ **「无」要认人话版**：提示词让模型只回一个「无」，但话多的模型会写成「这一章没有需要解释的背景知识点。」——那本是正常结果，报成「没按格式回」会让人以为坏了、再点两轮，每轮把整章重喂一遍。
  - **第一遍失败自动回退主模型（2026-07-27 加）**：`_rdBgScan(b, lines, doneTerms, mdl, fb)` 第 5 参是回退模型（`readerMarkBg` 传 `explainM`）。摘要模型多半配在另一个中转站，它一挂整个功能就废——而这一遍主模型也做得了、只是贵一点，所以换主模型再试一次，仍失败才算真失败。**回退按「段」独立**（长章切了几段，成功的段不会重发）；`fb.model === mdl.model` 或用户按停止（AbortError）时**不回退**，免得白烧一遍。
  - **标出来的词自动并进「名单」高亮**（2026-07-27 加）：用户原话「不然我一下子对应不上为什么这一段要标注背景」——词在正文里被点亮，一眼就知道这块背景冲着哪个词来的。`readerBgMergeNames(terms, quiet)` 规矩照抄 `readingAutoMergeNamesFromTables`（**只增不删**），收尾照抄 `readingSaveNamesFromEditor`：没样式就默认开着色、**`_readingMergeCache.text = null` 让下次渲染按新名单重包**、`readingRehighlightNamesInPlace()` 就地刷不跳位置。循环里用 `quiet:true`（紧接着的 `readingRerenderMsg` 会带上高亮，省一次全量重刷），整轮结束再统一刷一次给「同时载入的其它章节」。⚠️`readingSetNames` 的**长名优先排序不能破**（「明教教主」必须排在「明教」前，否则短名先匹配、吃掉长名一截）。名单是**本地的、不进云同步**（跟原有行为一致）。
  - **⚠️进度条 `#rdBgBar` 的层级和纵向位置都踩过坑（2026-07-27 修）**：① `z-index` 必须盖过聊天/阅读界面那层 modal（`.modal-overlay` 是 **200**）——原写 70，条子被整个阅读界面压在底下，**只有切到密钥/收藏/日历那些没开 modal 的页面才露出来**（用户报「打开阅读时候基本上看不到」，还因此误以为功能没启动）。240 照抄 `#translateActBar`（同类：浮在聊天界面里的固定条），仍低于底部弹层(997~999)和 toast(1000)。② 横向居中**别用 `left:50%+transform`**：那样元素可用宽度只剩半屏，`max-width:88vw` 吃不满、词名被截成「共…」；用 `left:0;right:0;margin:0 auto;width:fit-content`。③ **纵向位置：贴顶部，别再放底下（改了三版才定）**。v1 写死 `bottom:78px`，正好压住 clawd 上沿（三种屏幕全中，240 远高于 clawd 的 10，那一条就点不动）；v2 改成按 `.clawd-model-btn` 实时位置上移、留 12px，**矩形已经不相交了，用户仍报「真的需要再移上去很多」**——条子高 41px、z-index 240，够向 clawd(高 32px) 的那一路全被它吃掉，差几像素跟压着没区别。**底部是拇指区，这条只是「进度＋停止」，不该跟常用按钮抢**。v3 `_rdBgBarPlace()` 改贴 `#chatMessages` 顶端下方 10px（阅读区不可见时兜底 70），并**跟着 `#chatMessages` 的 scroll / window resize 重贴**——因为 `.chat-topbar` 随滚动收起展开会让整个阅读区上下移 51px，只在显示时贴一次的话条子会压住顶栏（顶栏 z-index 才 20）。翻译条 `#translateActBar` 同为 240，**判它在不在要认 `.show` 类**（它用 `opacity:0+pointer-events:none` 藏着、不是 `display:none`，看 `offsetParent` 会永远判成"在显示"）。复用同一节点，所以设 `top` 时必须把 `bottom` 显式置 `auto`（v2 设过），监听器在 `_rdBgBarHide` 里摘干净、否则标几轮积一堆。
  - **按小节标（2026-07-27 加）**：社科书一章三四万字，整章标一次又慢又贵、背景点还摊得散（用户原话「一章有3万多字，太多了」）。目录的**小节行右侧加 🏛**（`rbMarkBgSection` → `rbCloseShelf()` 后调 `readerMarkBg(bookId, chapIdx, secHeadP)`），右侧原来显示 `P段号` 的位置在标过之后改显示 `🏛 N 条`（按小节范围从整章 list 里分摊，别每节各扫一遍 reader_qa）。`readerMarkBg` 第 3 参 `secHeadP` 传小标题段号 → `rbSectionOf` 得出 `startP/endP` → `_rdBgScan(..., {from,to})` 只扫这一段。**⚠️段号全程用「整章编号」**，插入/渲染/reader_qa 都按整章算，改成节内相对编号会把块插到章首；同时把清单校验从 `1..lines.length` 收紧成 `from..to`，因为模型偶尔会引用节外段号。
  - **⚠️目录的小节段号原本比阅读器少 1（2026-07-27 修）**：`readerChapterPair` 把章节标题拼在正文前，标题占掉 P1、正文整体后移一段；而 `rbChapterHeads` 在**纯正文**上算段号，于是目录点小节会停在上一节末尾（实测「自我辩护的代价」目录算 P3、阅读器实际 P4）。修法是**只给结果加偏移** `rbChapterHeadOffset(ch)`，**不把标题喂进 `rbDetectHeads`**——否则标题行自己可能被判成小标题、凭空多出一行小节。新增 `rbSectionRanges(b,i)` 统一产出 `{p,title,startP,endP}`（阅读器编号），目录和标背景共用一套，避免两处各算一遍再对不上。
  - **⚠️空状态按哪个功能，两种书不一样（2026-07-28 改）**：社科书空着＝「导读」、背景让给长按；**小说空着＝「背景」、不需要长按**——小说本来就没有导读，那个空状态点了什么都不会发生，与其空着不如让给背景（用户原话「非社科类的书没有导读，把切换让给标背景」）。打字一律变「讨论」。按钮上的字统一叫**「背景」**不叫「标背景」（她要的字面）。所以 `readingOpenAskBar` 里 `_emptyLabel = _isSocial ? '导读' : '背景'`，长按那段开头 `if (!_isSocial) return;`。
  - **正文入口＝长按操作栏那个按钮（2026-07-27 加）**：用户嫌"标背景要翻目录、导读双击就有"。为什么不在操作栏加个 🏛：她要的是**零新增图标**的极简，而她真正喜欢的是「一个键、标签跟着状态变」那套（空着＝导读、打字＝讨论）。但"想听导读"和"想标背景"时输入框**都是空的**，程序猜不出来，所以第三态只能靠手势：**按住 500ms → 标签就地变「标背景」+ 底色变深（`.rp-bg-armed`），松手才真开始**——手势自己教会自己，且永远不会误点。范围**必须跟目录入口对齐**：社科书＝这一段所在的小节（直接把双击的段号丢给 `readerMarkBg`，里面的 `rbSectionOf` 会折算），小说＝整章（`rbDetectHeads` 在小说正文上会认出假小标题，按它切会跟目录对不上）。⚠️**长按的按钮必须关掉选词**：手机默认把长按理解成选词，会弹复制菜单、按钮文字被选中（用户实测「偶尔会变成复制选中」）——`.reading-actbar button` 上 `user-select:none` + `-webkit-touch-callout:none` + `touch-action:manipulation` 三件套，蓄力到位时再 `removeAllRanges()` 兜一次底。⚠️**两处 click 必须吃掉**：`pointerup` 后面紧跟一个 click，不拦就会在标背景之后再走一遍导读；**滑开取消也要拦**——按钮有宽度，手指滑 40px 可能还在按钮上，松手照样 click，那就成了「想反悔却走了导读」还照样花钱（写测试时真踩到）。
  - **⚠️讲解人设按书种走（2026-07-27 修）**：第二遍原来**一律**拼 `SOCIAL_MODE_PERSONA`，而那段人设开头就写死「我读的是社会学…**不是小说**」——读小说时标背景等于在骗模型（用户问「背景知识的提示有限定书本的种类吗」才发现；`rbIsSocial` 早就有，只是这里没用上）。现按 `rbIsSocial(b)` 二选一：社科书照旧（术语翻人话正对路），小说走 `BG_NOVEL_PERSONA`。**别图省事复用 `READING_MODE_DEFAULT_PERSONA`**——那是共读点评的人设（挑伏笔、聊角色、男生子设定、家族树画法），跟 `BG_EXPLAIN_INSTRUCTION` 的「绝不复述或解读原文」直接打架。`BG_NOVEL_PERSONA` 是最小版：同一个蘑菇声音＋「把书本之外的世界讲清楚」＋**小说特有的不许剧透**（「别提到我还没读到的后续剧情」——社科书没这问题）。**第一遍 `BG_SCAN_INSTRUCTION` 是书种无关的，不用改**：它找的是年代/制度/事件/学派/宗教/货币/真人，两类书通用。测试 `persona.js`。
  - 用户定的形态：手动触发、整章或**按小节**、**直接展开**（不折叠）、不设上限、跨章去重、**只讲书外的世界＋打比方、绝不复述或解读原文**（那是导读的活，两个功能刻意不重叠）。**⚠️改动这个功能后必须跑 `bash tests/run.sh`（2026-07-27 起）**：`tests/bg.test.js` 把上面每一条坑都钉成了断言（95 条，约 1 分钟，全程假请求、不花额度）。之所以专门给它建回归测试——这功能两天返工七八轮，**每一轮都是用户在手机上花着额度才发现的**（比如「第一遍改了 max_tokens、第二遍漏了」，用户那边表现为「只有第 1 条插进去，后面照样扣费」）。新增断言直接加进去；`playwright` 装在仓库外的 `~/.toolbox-test/`。
- **⚠️阅读摘要截断上限 40000（别调回 16000）**：`readerSummarizeChapterOnce` 里 `body.slice(0, 40000)`。原值 16000 对网文（三五千字/章）绰绰有余，但社科书一章 34000~39000 字，**一半以上正文被静默切掉、摘要只概括了前半章**（2026-07-27 修）。短章节从不触及此分支。
- **共读正文可调**：正文字号（`--reading-fs`/`reading_font_scale`）、段间距（`--reading-pspace`/`reading_para_space`，2~28px 默认8）。段落带行内 `margin:0.5em 0`，改间距的 CSS 必须 `!important` 才盖得过行内样式。批注框 `blockquote[data-cp]` **只吃自身上边距、贴住它点评的那一段**，不随段间距推开。
- **翻译模式追问**：长按译文块触发（约 0.5s，移动超 10px 取消），双击/拖选留给浏览器选词复制。
- **等待中可中止**：翻译、翻译追问、共读提问加载时按钮带 `✕`，用 `AbortController` 掐断；中止不弹报错。
- **遮罩**：`chat_mask` localStorage（'1'/'0'）控制 `body.chat-mask-on`，初始化按保存值强制对齐。
- **夜间模式**：clawd「主题」子面板 🌙 开关（`setNightMode`，存 `toolbox_night`）。每个浅色皮肤有 `.theme-xxx-night` 类，**7 个夜间皮肤共用同一套深蓝底**，只强调色不同；`深林绿(dark)` 无夜间版。`applyTheme` 里 `cls = (nightMode && t.night) ? t.night : t.cls`。
- **共读点击翻页按行对齐**（`readingPageDown`）：用 `Range.getClientRects()` 找「本页最后一行完整露出的行」滚到容器顶部——新页顶行 = 上页读到的最后一整行（保留一行接头），永远不切半。找不到行或目标太近(<60px)退回整屏滚。极小瑕疵：开「画框」时顶边可能露上一行画框底描边 2~4px，文字不切。
- **用量/计费面板**（clawd「💰 用量」→ `apiLogShowPanel`）：API 调用汇集在 `chatStreamChat`（后台小任务）和 `chatDoRequest`（主力流式）两处，各装探针 `apiLogRecord(purpose,model,inTok,outTok,est)`。token 优先取中转站真实 `usage`（两处都加 `stream_options:{include_usage:true}`，解析 `apiUsageFromChunk`），缺则 `estTokens` 估算并标 `est`。每个调用点带 `purpose:` 标签。存储本机不同步：`toolbox_api_log`（明细封顶500）、`toolbox_api_agg`（累计聚合）、`toolbox_api_price`（单价/百万token）。
- **Prompt Caching（2026-06-30 加）**：Claude 走原生 `/v1/messages` 时启用。`aiOpenStream` 加头 `anthropic-beta: prompt-caching-2024-07-31`；`aiBuildAnthropicBody` 给稳定前缀（整段 system + 末条消息最后一个 block）打 2 个 `cache_control:{type:'ephemeral'}`。计费把 `cache_read/creation_input_tokens` 都加进 `inTok`，单独透出 `cache_read`；面板顶「⚡ 缓存命中 X tok · 约省 ￥Y」。**排查：连续多轮命中一直 0，多半是中转站没回传缓存 usage，不是没缓存。**
- **云同步架构（CF 主力）**：CF 走「逐卡片 + 清单(manifest)」（`cfBuildManifest`/`cfSmartSync`），**整包 blob（`buildSyncPayload`）只给 Gist 和本地快照用，CF 不碰**。想让某项数据在 CF 同步，必须挂进 **manifest**、不能只塞 blob。密钥/收藏夹逐卡片（`k_`/`s_` + `cardTimestamps`）；书签、字体、阅读进度、收藏模型等「整块按时间戳后写覆盖」（能正确传播删除）。
- **⚠️对话记录是同步的，别再被那句过时注释骗了（2026-07-31 记）**：`exportAllData` 里那句「共读/对话记录与阅读书库不参与云同步」**早就过时**，对话云同步是后来加的。**云端结构跟别的数据完全不同**：不进 `_manifest`，而是①一个对话一条 `chat_<对话id>`；②**自己的清单** `_chat_manifest`（`{chats:[{id,title,model,updated,msgCount,pinned}]}`）；③超过 `CF_CONV_PLAIN_MAX`(1.4MB) 自动切块 `chat_<id>::p0..pN`（绕 D1 单行 2MB 上限），读时拼回。所以**用 `chat_history`/`convs` 去 grep 或探云端键，一个都探不中**（那是本机 IndexedDB 的键名）——实测在用户云端捞到 27 个对话，含 244 条消息的「翻译窗口」。相关：`chatSyncPush`/`chatCloudPull`/`cfPutConv`/`cfGetConv`，防缩水（云端条数多就不推）、删除走 `chat_tombstones`(30 天)。**教训**：注释和代码打架时以代码为准；用户说「明明能」的时候，先按她说的去找，别拿注释当证据反驳她。
- **收藏模型同步（2026-06-20 修）**：`chat_fav_models`（含每模型备注/价格 `f.price`）经 `chatFavModelsSave()`（写本地+盖 `_ts`+防抖 `syncPushData`），挂进 `cfBuildManifest`、按 ts 覆盖。**单价存在收藏对象里，收藏一同步计价价格就跟着同步。**
- **书籍/字体同步（自建服务器）**：正文/字体太大不进 CF，走自建 cc-books 服务器同步，前端函数前缀 `bkSync*`。书是「本地为主、服务器当镜像、按需下载」，占位书 `_remote:true` **不落盘 IndexedDB**；字体是「换设备启动自动全量拉回」。**服务端细节见私有 `SERVER-OPS.md`。**
  - **⚠️下载大书：进度 / 超时 / 失败原因（2026-07-30 加）**：用户报「这种大文件，我的手机同步半天都同步不下来」。最难受的不是慢，是**分不清在下、卡死了、还是早就失败了**——老 `bkSyncDownload` 就是 `fetch().then(r => r.json())`：没进度（等整包下完才动）、没超时（手机网络一卡永远挂着，同 `chatStreamChat` 那个坑）、失败原因全丢（`.catch(function(){ cb(false); })`，界面只写「请检查配置或网络」，而配置好着，**白让她去翻设置**）。现在流式读 `r.body.getReader()` + `onProg(已下,总共)` + `BK_DL_STALL_MS=60000` 无新字节自己掐断 + `cb(false, 原因)`，返回 `AbortController` 供「停止」。⚠️**三种失败必须分开说**：主动停 / 网络没响应 / 服务器报错——含糊一句会让她顺着错的方向反复试（同收藏模型那次「A 坏了却报 B」的账）。⚠️**判据是「多久没动」不是「跑了多久」**：慢但在动的网络不该被掐。⚠️**总量只能读 `X-Raw-Length`，不能读 `Content-Length`**：Caddy 给这条响应开了 `encode zstd gzip`，`Content-Length` 是**压缩后**的大小（边压边发时干脆没这个头、走 chunked），而 `response.body` 流里读到的是**解压后**的字节，两个数对不上、进度条会冲过 100%。服务端 `cc-books/books.py` 的 `_json` 里加了 `X-Raw-Length`，并且**必须配 `Access-Control-Expose-Headers`**（工具箱在 tool.* 、书库服务在 cc.* ，跨域自定义响应头不 expose 前端读不到）。测试 `tests/bkdownload.test.js`（10 条，假 fetch、不联网）。
  - **⚠️`Toolbox/server/books.py` 是过期副本，别照它改**（2026-07-30 记）：线上真正在跑的是 `/home/ubuntu/cc-books/books.py`（systemd `cc-books.service`），它比仓库这份多了 `folder` 字段 / `/setfolder` / `X-Raw-Length`。仓库那份留着做参考，**改服务端务必改 `/home/ubuntu/cc-books/books.py` 并 `sudo systemctl restart cc-books`**。
  - **字体没有定时同步**，只有三个触发点：冷启动（`idbInit` 之后）、打开书架（`rbOpenShelf`）、展开字体面板（`readingToggleFontPanel`），且有 4 秒节流。「传了半天另一台设备没有」先问是不是压根没触发，别急着查服务器。`bkSyncCfg()` 缺 url 或 token 时**所有 bkSync* 直接返回、不报错不提示**——书和字体共用这套配置，所以「书架能看到服务器的书吗」是判断配置好没好的最快问法。
  - **删除必须两头都删**：只删服务器，本机 IndexedDB 那份会在下次 `bkSyncPullFonts` 被当成「本地独有」补传回去；只删本机，服务器那份会被当成「新字体」拉回来。两个方向都是「删了又自己回来」。`readingDeleteFontAny` 是唯一同时处理两头（外加清空引用它的槽位）的入口，别绕开它。
- **字体槽位（7 个）**：`READING_FONT_SLOTS` = 正文/引号/短句/标题/人名/精句 + **缺字**。前六个各写一个 CSS 变量；「缺字」（`isFallback:true`）不写变量，而是接在**装饰槽位**字体链尾巴上（`"主字体", "缺字字体", serif`）——**正文那条链不接**，用户明确要求正文一个字不动。花体做子集常漏生僻字（「苼」在 GBK 不在 GB2312，实测国产花体基本都不收），浏览器逐字回退，主字体有的字轮不到兜底。槽位选「默认」时**照旧不写变量**：靠 CSS `inherit` 继承父级算好的整条链，兜底会跟着继承过来，在这里补反而会切断继承。
  - ⚠️ **加新槽位必须同时改两处**：`READING_FONT_SLOTS` 和 `cfBuildManifest()` 里那个**手写死的** `fontSlots` 对象（不是从槽位表推的）。只加前者的话，`readingApplyFontSync` 会在云端较新时把新槽位 `removeItem` 掉，表现是「当场好好的、过几小时自己消失」，肉眼根本查不出来。测试 `tests/font.test.js` A 组钉住。
  - **⚠️「标题字体」只作用在 `.reading-merged .reading-chap-title` 上，而这个 class 原本只靠 `READING_TITLE_RE` 猜（2026-07-30 修）**：那条正则要求同时满足「`第X章/节/回/篇/卷/话` 开头 + 后面 ≤20 字 + 不含逗号句号问号顿号」，所以**凡是不长成传统章回体的章名一律拿不到标题字体**——用户导进来的知乎存档三本，章名是「【商业经济】教育家 - 为什么，这几年…？」，连改写成「第一章 为什么，…？」也照样不认（有逗号问号）。**数据侧无解**：要过正则就得把章名砍到 20 字内且不带标点，那既看不出讲什么、也搜不到了。修法是**阅读模式不用猜**——`readerChapterPair` 本来就把章节标题拼在正文最前面（「标题占掉 P1」），所以 `chatReaderMode && p === 0` 直接认，和原正则**并联**（共读模式一字不动：那边正文是发进来的，P1 不保证是标题）。⚠️另加 `length <= 120` 兜底，万一某本书 P1 不是章名而是正文首段，别把一整段套上花体糊成一团。⚠️**`_readingMergeCache` 的签名必须带 `rdMode`**：同一段文字在两种模式下该不该包这个 class 不一样，不带会串出**隐形的错样式**。测试 `tests/chaptitle.test.js`（7 条）。
- **窗口化渲染（进会话防卡顿）**：长篇会话进入只渲染「上次阅读位置→末尾」窗口，早章上滑到顶自动补渲染。`chatRenderAllMessages(windowed)`（**只进会话那次传 true**）、`chatComputeRenderStart`（以 `reading_pos_<convId>.idx` 为锚、前留 `READING_RENDER_BUFFER=4`）、`chatLoadEarlier`（补渲染并用高度差修 scrollTop）。**阅读定位靠「第几条消息+第几段」锚点（`.reading-merged p[data-p]`），不靠像素，所以少渲早章不影响回原位。**
  - **秒出 + 后台补（2026-06-20 修）**：先同步画 `READING_RENDER_FIRST=18` 条让界面秒出，其余 `setTimeout` 每批 `READING_RENDER_FWD_BATCH=8` 条后台补、接末尾（不移动已定位）。`_chatRenderToken` 代次令牌作废在途补渲染。用 `setTimeout` 不用 `rAF`（页面隐藏时 rAF 暂停）。
  - **双向窗口化 + 裁剪（2026-07-08 修，治「提前发十几章滑到最顶卡+烫」）**：早先只封窗口「起点以上」，起点以下一直渲染到全书末尾——于是把十几章(≈20万字)提前发出去、再滑到最顶从头读时整本书都堆进 DOM，滑动卡、手机烫。现在**起点以下也只先渲染 `READING_RENDER_FORWARD=12` 条**，底部挂 `chatMakeLaterSentinel`「↓ 载入后面章节」，滑到接近底部自动 `chatLoadLater` 补、对称于顶部的 `chatLoadEarlier`。`chatRenderEndIdx`＝已渲染的「末条+1」(==`chatMessages.length` 即到真末尾)。窗口超 `READING_RENDER_MAX=24` 条就从**远离视口那端裁掉**（`chatTrimTopIfNeeded` 带 scrollTop 高度差补偿 / `chatTrimBottomIfNeeded` 在视口下方无需补偿），实测全程 DOM 恒定 ≤~20 条、不随发章数增长。
    - **发新章/流式/生图接末尾前必须 `chatEnsureRenderedToEnd()`**（补齐窗口到真末尾+删底部哨兵），否则新节点接到哨兵后头、和未渲染的中间几条**串位**。已接：`chatSend`、`chatDoRequest` 收尾、`chatInsertGenImage`；这些追加后都要把 `chatRenderEndIdx = chatMessages.length` 同步、并 `chatTrimTopIfNeeded`。截断/删除(`chatSheetTruncate`/`chatSheetDelete`)按新长度校正 `chatRenderStartIdx/EndIdx`。
    - **任意位置跳转前先 `chatEnsureRenderedIdx(idx)`**（窗口上方→`chatRenderUpTo`、下方→`chatRenderDownTo`），否则目标不在 DOM、`querySelector` 返回 null 定位失败。已接：`chatNavJump`、`readingJumpBookmark`、`readingJumpHighlight`。
    - **⚠️阅读模式书签/划线必须按「章节号」匹配（2026-07-19 修「每章固定位置冒幻影标记」）**：书签(`reading_bookmarks`)/划线(`reading_highlights`)按 convId 存、原本只记 `msgIdx`+段号。共读会话单调增长 msgIdx 稳定没事；但**阅读模式(`chatReaderMode`)每次开章临时重建会话，同一 msgIdx 会对上不同章**——历史某 msgIdx 的标记就漏到每一章相同段号上。修法：保存时同时记 `chap: _readerChapOf(msgIdx)`（=该消息 `readerChapter`）；`_markMatchesMsg(mark,msgIdx,chap)` 在阅读模式按 chap 匹配、共读按 msgIdx。涉及 `readingMarkBookmarks`/`readingApplyHighlights`/`readingIsBookmarked`/`readingRefreshBookmarkUI` + 保存点(`rdHlCreate`/段落书签 toggle/`readingAddQuestionBookmark`) + 面板条目带 chap + 跳转(`readingJump*` 走 `_readerJumpTo`：没载入就 `readerOpenChapter` 再轮询滚到位)。旧数据无 chap → 阅读模式不显示/跳转给提示（它们本就错位，丢弃即可）。测试 `test_reader_marks.js`/`test_reader_jump2.js`。
    - **⚠️阅读模式读痕跨设备同步（2026-07-21 修「共读能同步、阅读不能」）**：根因是**存储键跨设备不稳**——共读会话 id `chat_<...>` 跨设备一致，阅读是 `reader_<本地bookId>` 而 `bookId='bk_'+时间戳+随机` **每台设备各生成**（导书/下载都会本地新 id，见 `bkSyncDownload`/`rbAddBook`），所以同一本书读痕对不上号。修法三层：① 新增 `readingMarksConvId()`——阅读模式(reader_ 前缀会话)把读痕键换成跨设备稳定的 `reader_<文件名|大小>`(=`readerBookKey`，与 `reader_qa` 同款)，8 处读痕存取点统一走它；`readerMigrateMarksKey`(进书时)+`readerMigrateAllMarksKeys`(开机 idbInit 后全量扫描)把旧键数据幂等搬到新键并重推。② **读痕面板「提问」在阅读模式直接从 `reader_qa` 生成**(`rdTraceReaderAskEntries`)——它与正文问答同源、已证实能跨设备同步(正文有问答=读痕就有)，彻底绕开易丢的 `reading_bookmarks`；按章#段聚单问/追问串、答案取 `reader_qa`、章名取书章节标题、跳转按章(msgIdx 可空)。共读/翻译仍走原书签路径。③ **`reading_bookmarks`/`reading_highlights` 云端合并由「整块按时间戳覆盖」改为「逐会话键取并集」**(`_mergeMarksBlobs`+`_readerMergeMarks`，签名去重、同源同 ts 不重复)——旧覆盖式一台设备一保存就把另一台读痕整块盖没(这是跨设备丢失的另一根因)；并集有新增则回推(cfSmartSync 里 `marksDirty`)，能自愈此前被覆盖的数据。**代价**：并集不传播删除(划线删除可能被另一台复活；但阅读模式提问删除由 `reader_qa` 每章 LWW 正常传播)。测试 `test_marks_key.js`/`test_sweep.js`/`test_reader_qatrace.js`。
    - **⚠️阅读模式正文问答换设备要「重灌」（2026-07-22 修「读痕有内容但正文看不到问答」）**：`reader_qa` 按稳定键跨设备同步、是权威；但正文靠 `readerChapterPair` 在**开章那刻**从 `reader_qa` 现拼成 `assistant.content` 快照，`chatSaveCurrentConv` 存的是那份快照。换设备时设备B常在「问答还没同步来」的空窗期就开过书、存了**空版正文快照**，之后 reader_qa 才同步到（→读痕面板直接读 reader_qa 有内容，正文快照仍空）；而「继续阅读」走 `chatLoadConv` 每次都从空快照恢复、不回灌。修法：新增 `readerHydrateLoadedQA()`，`chatLoadConv` 命中 `reader_` 会话时（**渲染前**、`if(chatReaderMode)` 块内）用 reader_qa 把每章 `readerChapter!=null` 消息的 content 重拼，不打乱滚动锚点。开章路径本走 readerChapterPair 现拼、不受影响。**遗留边角**：正在读某章时才同步进来的新问答，得重开该章才显示（没做实时重渲染，避免打断阅读位置）。测试 `test_reader_hydrate.js`。
    - **📌 同步这类 bug 的三个共性（排查心法，2026-07-22 记）**：这两天修的读痕键对不上、整块覆盖盖没对方、正文用旧快照——都是同一类毛病的侧面。共性①**存储键跨设备要稳**：凡按「本地随机 bookId」或「device-local convId」当键的数据，换设备必对不上号，得换成「文件名\|大小」这类内容派生的稳定键。共性②**合并别用「整块按时间戳后写覆盖」**：一台一保存就把另一台没同步上来的整块盖没；能拆成「逐条/逐会话键取并集」就并集（代价是不传播删除，删除另想显式墓碑）。共性③**别信「保存那刻的快照」**：快照可能是同步到齐之前拼的，载入时要用权威源重灌（reader_qa/记忆表/名单等都算权威源）。**待查隐患候选**（还没逐个查、碰到再对着看）：`reading_pos_*` 阅读进度键仍带本地 convId？收藏/密钥删除靠墓碑传播是否可靠？记忆表格/摘要的空窗期快照是否也需重灌？划线删除被并集复活的老问题。
    - **⚠️阅读模式必须绕开窗口化装载器（2026-07-19 修「下一章还是本章」）**：`chatLoadLater`/`chatLoadEarlier` 开头都加了 `if (chatReaderMode) return;`。因为阅读模式 `chatReadingMode` 也为真、且**不维护 `chatRenderEndIdx`**（停在初值 2），滚到底部时**窗口化 `chatLoadLater` 会把 `readerAppendNextChapter` 刚接的下一章又渲染一遍**（DOM 里那章出现两份）→ 读完这一章往下还是这一章。阅读模式接章只走 `readerAppendNextChapter`/`readerPrependPrevChapter`。测试 scratchpad `test_reader_next.js`/`test_reader_dup.js`。
    - **⚠️阅读模式(`chatReaderMode`)必须绕开 `chatEnsureRenderedIdx`**（函数头 `if (chatReaderMode) return;`）：阅读模式 DOM 由 `readerOpenChapter`(非窗口化)+`readerAppend/Prepend` 自己管、**不维护 `chatRenderEndIdx`**（停在初值 2）；若介入会误判「后面没渲染」而把已在页面里的章节**重复补一遍**、撑出重复 DOM → 读痕定位不到出处（2026-07-08 回归，已修）。测试 scratchpad `test_reader_jump.js`。
    - 测试脚本 scratchpad `test_window.js`（注入仿真消息，验证初始窗口、上下滑裁剪、无空洞、发章、跳转）。
    - **⚠️别再试「松裁」**：2026-07-08 曾把 `MAX` 放宽到 60、`CHUNK` 降到 4（想对齐阅读模式的「只加载不裁剪」），**用户实测手感不如狠裁版**，已回退。保持 `READING_RENDER_MAX=24` + `CHUNK=8`（狠裁：全程 DOM≤~20 条），别再往回改。
  - **⚠️整章 content-visibility 试过又移除了（2026-07-08 当天加当天撤）**：曾给整章加 `.reading-chapter{content-visibility:auto}` 防卡/防烫，但**屏外整章高度变估算值，让「记住/回到上次阅读位置」定位跳歪**（`readingSaveScrollPos`/`readingRestoreScrollPos` 靠 `getBoundingClientRect` 精确量每段真实位置；屏外 cv 元素量出来是估算值→存错锚点/恢复到错位置）。**和更早在「段落」上试 cv 因同样原因退回是同一个坑**（用户报「长按🔍打开上次共读、回到上次读到的地方跳错位」）。**别再加**。长篇流畅度只靠「窗口化」：页面里始终只留眼前约 20 条**真实排版**的消息，真实排版=定位天然精准（这也是老代码一直坚持的路线）。
