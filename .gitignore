# Claude Code 本地配置/缓存（不属于项目代码）
.claude/

# 服务器运维备忘（含公网 IP/端口/路径等敏感拓扑，只留服务器本地、绝不上公开仓）
SERVER-OPS.md

# 她自己的阅读底图。⚠️**故意不入库**：这个仓库是 public，她传的图不该跟着公开。
# 但部署脚本是 `cp -rf bg/.` 整个目录复制、不看 git，所以线上照样有。
# ⚠️代价：重装/重新 clone 时这个目录不会跟着回来，图得重新放一次。
bg/mine/
